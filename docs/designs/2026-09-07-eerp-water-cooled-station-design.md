# 电驱动水冷冷站 EERp 计算与研发评价设计

## 1. 状态与使用方式

状态：用户已批准本文并授权编码；后端已形成研发活动候选。第 2–12 节保留已确认设计边界，第 13 节记录实际契约、实现及验证限制。当前产品状态以 [PROJECT_STATUS.md](../../PROJECT_STATUS.md) 为准，未将本候选提升为正式版本。

设计已按本文的范围、必要依赖、专业输入和验收边界确认。实现不得通过绕过质量门禁、读取其他模块内部表或扩大产品范围来完成表面闭环。软件验收、专业确认、正式版本批准与现场验收分别记录。

## 2. 已确认范围与非目标

### 2.1 已确认范围

| 项目 | 边界 |
| --- | --- |
| 场景 | 中央空调中的电驱动水冷冷站，支持配置多台冷机、冷水泵、冷却水泵和冷却塔 |
| 指标 | 首批仅 EERp 冷站系统能效比 |
| 统计 | 完整自然年度，按建筑的版本化时区生成半开区间 |
| 冷量来源 | 冷量表累计量；流量与供回水温度计算冷量 |
| 介质 | 水；物性参数及适用条件有来源、版本和确认状态 |
| 来源选择 | 同一覆盖范围与时段只有一个权威来源，不重复计量，不自动故障切换 |
| 电量 | 消费上游同周期的原生耗电量及其计量边界证据 |
| 评价 | 按专家参考材料中的全年阈值开展明确标注的研发评价 |
| 交付 | 后端配置、审核、计算、结果查询、追溯和显式有界重算 |
| 验证 | 确定性合成样例、公共契约联调及隔离目标数据库验证；不假定真实现场数据可用 |

不足全年或年度输入不完整时，只提供已有冷量、电量、有效区间、覆盖情况和缺失原因，不生成完整年度 EERp，不套用全年评价阈值。

### 2.2 本期不包含

- EERs、EERr、COP、WTF、EERt、WRef、EERhp 及能耗强度、空调碳强度指标的新增实现。
- 典型工况评价、月度等级评价、排名、诊断、告警或优化策略。
- 风冷、吸收式、蓄冷、外购冷、冷热联供、非水介质和未经确认的共用设备分摊。
- 多能源折算电、折标煤算法扩展、碳因子维护、光伏、绿证或动态碳因子。
- 任意公式、脚本、复杂权重等通用评价引擎。
- 业务页面、自动报表、设备协议采集器、控制和指令下发。
- 冷量表与计算冷量的自动比对诊断、自动择优或故障切换。

新增指标、设备类型或业务页面须另行确认；不能因附件列出了完整指标表就自动实施全表。

## 3. 资料依据与评价规则

### 3.1 证据分层

主参考为用户提供的《GB/T 17981-2025〈空气调节系统经济运行〉指标体系·开发参考》，重点是第 3 节指标表、第 5 节判定和第 9 节口径事项。本文保存本期所需的规则摘录及解释边界，不提交附件原件或个人存储路径。

[国家标准官方信息页](https://openstd.samr.gov.cn/bzgk/std/newGbInfo?hcno=054E25A1C1B52CC054F50A9653DBD5A5)已核实标准名称、编号和实施信息；全文公式、表注和适用条件尚未逐条复核。专家参考材料已经给出可执行的研发比较规则，正式原文复核状态不能被误写成“没有评价标准”。

[OIML R 75-1 第 8 节](https://www.oiml.org/en/files/pdf_r/r075-1-e02.pdf)仅作为流量、温度与热量积分关系的技术参考，不替代 GB/T 17981 的适用性确认，也不表示平台软件取得计量器具认证。

### 3.2 EERp 规则摘录

| 项目 | 专家参考材料内容 | 本期处理 |
| --- | --- | --- |
| 分子 | 制备总冷量 | 固定制备端边界与计量位置 |
| 分母 | 冷机、冷水泵、冷却水泵和冷却塔风机耗电，不含末端 | 对照配置逐项检查覆盖和去重 |
| 方向 | 越大越好 | 固定为正向能效比较 |
| 全年累计 | 引导值 4.0，先进值 5.0 | 本期研发评价使用 |
| 典型工况 | 引导值 4.2，先进值 5.2 | 仅保留资料说明，不实现本期评价 |
| 比较符号 | 应大于引导值，宜大于先进值 | 严格使用 `>`，不改为 `>=` |

原文核实前，规则来源类型为 `EXPERT_REFERENCE`，标准核实状态为 `METADATA_VERIFIED_TEXT_PENDING`，使用性质为 `DEVELOPMENT_REFERENCE`；上述字段不能因配置审核通过自动升级为正式标准核验。

### 3.3 全年研发评价区间

| EERp 值 x | 结果代码 | 结果说明 |
| --- | --- | --- |
| x < 4.0 | BELOW_GUIDANCE | 低于引导值；参考材料第 5 节称“急需节能降耗” |
| x = 4.0 | AT_GUIDANCE | 等于引导值，未满足严格大于引导值的要求 |
| 4.0 < x <= 5.0 | GUIDANCE_ONLY | 满足引导要求，未满足严格大于先进值的要求 |
| x > 5.0 | ADVANCED | 满足先进要求；参考材料称“先进节能” |

“等于引导值”和中间区间采用客观比较描述，不声称这些名称是标准正式等级。EERt 的末端阈值不进入本规则。不得套用参考材料总述中不区分方向的“超了引导值”说法。

比较基于计算精度下尚未作展示舍入的值；保存原值、展示值和舍入版本。配置只允许 EERp 的有限规则结构，校验先进值大于引导值，区间无重叠且覆盖所有合法结果。新规则版本须有来源和审核，不能修改已有版本以改变历史等级。

## 4. 既有约束与代码复用边界

| 既有约束或能力 | 处理 | 具体含义 |
| --- | --- | --- |
| Spring Boot 单体、Vue 单工程 | 保留 | 同进程通过 Java 公共端口协作，不增加微服务 |
| 现有建筑、空间、系统、设备、测点和关系治理 | 保留 | 引用现有身份与关系版本，不复制资产主数据或建立第二棵关系树 |
| 场景化 Q0/Q1/Q2 门禁 | 保留 | 复用策略解析和审核；不自行允许 Q1/Q2 |
| 第七闭环累计量、积分、计量事件、周期与汇总算法 | 保留并适配 | 通过公共契约复用；新增制备冷量的原生量入口，不复制算法 |
| 第七闭环既有能源使用范围和折标规则 | 保留 | 不将制备冷量伪装为外购热力，不修改既有 tce 结果含义 |
| 继承的分钟 COP 与展示接口 | 保留 | 不将分钟平均输入或 COP 中间值直接认定为年度冷量事实 |
| 碳管理、已合并电力因子选用规则 | 保留 | 本期不消费或修改碳因子，不重命名 CO2/CO2e 单位 |
| 前端可视化任务与 Modbus 适配器候选 | 保留 | 本期不接管页面或协议实现，不假定其现场数据已可用 |
| 冷量公共事实入口、派生量及原生量周期接入 | 待补齐 | 属于本任务必要依赖，以下章节明确边界 |
| 生产专业参数与标准全文适用性 | 未解决 | 独立待确认，不通过研发配置或软件测试替代 |

当前代码依据包括：[ChillerCopFormula](../../src/main/java/com/platform/iot/formula/ChillerCopFormula.java)、[FormulaInputAssembler](../../src/main/java/com/platform/iot/formula/FormulaInputAssembler.java)、[EnergyActivityDataService](../../src/main/java/com/platform/energy/activity/EnergyActivityDataService.java)、[EnergyAggregationCore](../../src/main/java/com/platform/energy/aggregation/EnergyAggregationCore.java)、[EnergyPeriodCalculationService](../../src/main/java/com/platform/energy/period/EnergyPeriodCalculationService.java)和[计量边界汇总 DTO](../../src/main/java/com/platform/energy/summary/api/EnergySummaryContracts.java)。

现有冷机公式使用固定水密度、比热和分钟平均输入；聚合核心已有累计量、阶梯/梯形积分、最大间隔和覆盖检查。能源活动接口要求能源专业属性，不能直接把温度和流量测点当成能源活动量。周期服务与折标流程存在关联，因此不能把已有方法的存在当作冷量全链已接通。

## 5. 冷站配置和计量覆盖

### 5.1 配置对象

EERp 配置引用一个建筑内的冷站系统身份和计量边界。至少包括：冷站类型、关系版本、设备及测点引用、覆盖对象、制备端计量位置、电量分项角色、冷量来源、业务生效区间、来源证据、研发性质和修订号。多台冷机和多组泵按配置处理，不写死数量或测点编码。

配置必须说明冷量来自单机出口组合还是冷站制备端总表。制备端与交付端不能凭单位相同而互换；旁通、混水或测点不在同一水路时，不得自动配对。共用设备在没有经确认的独占归属或上游分摊结果时保持未分配，本期不新增分摊算法。

### 5.2 去重和来源切换

- 电量选择完整总表或互不重复的分表组合；总表和覆盖于其中的分表不得重复计入。
- 冷量选择冷站总量或互不重复的机组制备量；总量和机组分量不得同时相加。
- 权威性以覆盖对象和生效区间校验，不能仅通过测点 ID 不同认定互不重复。
- 每个覆盖对象同一时段选择 `METER_CUMULATIVE` 或 `FLOW_TEMPERATURE`；不同非重叠机组可有不同来源，但必须合计完整覆盖冷站。
- 来源变更须审核并给出明确生效边界；分段不可重叠，出现缺口时年度结果不完整。不得以主源失败为由自动使用副源补齐。
- 配置、关系、介质或处理策略在年内变化时，按实际生效时间分段，固定各段版本；不按天数比例拆分无法提供边界读数的累计量。

## 6. 输入契约和模块职责

拟由 `com.platform.energy.efficiency` 承担冷量组装、EERp 和有限评价；采集、质量、关系、能源字典与周期模块仍拥有各自事实。下列端口名称为拟新增契约，不表示当前已实现。

| 公共契约 | 提供方与输入 | 必须返回的证据 |
| --- | --- | --- |
| 冷量计算测点读取端口 | 采集与质量模块；建筑、显式测点集合、有界半开区间、读取水位和游标 | 数值、物理量类型、原始单位、事件/接收时间、事实身份、数据来源性质、质量等级/门禁决定/策略版本、允许的边界锚点及拒绝区间信息 |
| 冷站配置与历史覆盖解析 | 现有资产和关系公共服务及本模块配置；建筑、冷站、区间、明确版本 | 设备/水路/测点角色、计量位置、覆盖对象、总分关系、来源选择和确认状态 |
| 原生量聚合与周期提交端口 | 能源聚合/周期模块；已固定证据的累计量或派生制冷功率 | 数量、单位、区间、质量与完整性、计量事件/积分版本、不可变快照及封账状态 |
| EERp 周期输入端口 | 原生量周期与边界汇总模块；冷站、年度、已固定快照集合 | 同周期冷量和电量、覆盖对象、单位、关系/汇总版本、有效与缺失区间、例外、结果性质和可读取输入证据 |

冷量计算测点契约必须执行建筑权限、点归属、物理量及单位匹配和计算场景质量门禁。不能让客户端自选宽松场景；查询展示策略不能代替计算策略。新场景默认沿用无正式策略时仅允许 Q0 的安全行为，空允许集合仍表示全部拒绝。对齐处理标记与 Q 等级分别保存，不把保持值自动标成实测 Q0。

新的温度/流量读取实现放在上游模块，由上游访问其存储。下游不得使用上游 Repository/Mapper，不使用旧页面曲线 DTO 作为计算输入，也不通过 HTTP 回调本应用自己。

制备冷量和制冷功率须有显式物理量身份、单位和版本，派生量记录三路来源依赖，不能伪装成设备直接上报事实。首版归一输入为 `m3/h` 和 `degC`，输出为 `kW`、`kWh`，电量归一为 `kWh`；已有支持的固定比例换算使用公共单位契约。温度、流量和制备冷量缺失的元数据支持仅作必要增量，不扩展任意单位表达式或折标品种范围。

周期模块增加明确的原生量模式，使制备冷量无需 tce 规则即可形成周期事实；不得通过伪造折标参数或全局忽略 `TCE_RULE_UNAVAILABLE` 实现。已有折标链及其审核、封账语义保持原样，新增入口必须有独立契约和兼容回归。

## 7. 冷量计算、时间与质量

### 7.1 冷量表路径

使用上游累计量算法处理起止锚点、复位、回绕、换表和已审核修正。不使用最大读数减最小读数替代事件处理，不把负差自动清零。累计量两个有效端点不等于中间每个采样时刻都有记录：是否足以证明该段总量完整，应依累计量语义、计量事件及相应质量规则判断，不能套用温差积分的采样覆盖率。

### 7.2 流量与温度路径

在水介质与已确认参数适用范围内，采用固定参数近似式：

```text
P_c(t) = rho × cp × Vdot(t) × [T_return(t) - T_supply(t)] / 3600
Q_c = 对 P_c(t) 按时间积分
```

`rho` 单位为 kg/m3，`cp` 为 kJ/(kg·K)，`Vdot` 为 m3/h，温差为 K，制冷功率为 kW；积分时长以小时计，冷量为 kWh。流量计在供水侧或回水侧的安装位置与介质参数适用条件一并保存。

密度、比热、适用温区和处理规则必须来自同一明确参数版本。首批不实现动态物性库；超出有效参数范围时拒绝该段。既有 1000 和 4.18 常数仅可作为显式研发样例参数，不能直接继承为所有现场的正式默认。

### 7.3 对齐与积分

- 精确同步样本直接组合；非同步样本仅在已配置并审核的有限时长保持前值规则下组合，不使用未来样本向前填充。
- 时间节点包含相关测点更新、允许保持时长到期、质量拒绝、配置生效及区间边界；保持值必须保留原始事件时间和使用时长。
- 每路样本必须满足允许时差、最长保持时间和自身质量门禁。被拒绝或失效的样本不能通过过滤后连接前后两个“好点”而被跨越。
- 先组装对齐后的流量和温差，再计算制冷功率。不能将各路独立分钟均值、月均值或年均值相乘作为权威冷量。
- 积分复用既有算法，固定积分策略版本。前值保持输入与阶梯积分配套；梯形积分只用于规则明确允许的端点序列，不能跨配置变化、保持到期、缺测或异常段进行平滑。
- 长缺口拆为无效区间，不在缺口两端连线积分。时间精度使用事件毫秒与精确时长，适配时不得因现有秒级计算而静默丢弃亚秒时长；精度不足须明确拒绝或补齐公共算法契约。
- 年度输入按有界段处理，处理段边界和年度/月度边界均不得重复积分或遗漏；不得每次年度查询扫描全年原始数据。

### 7.4 零值、负值和完整性

负流量、反向温差、非法单位、非有限值、介质越界、不同水路以及未确认映射均返回明确拒绝原因，不取绝对值或补零。零流量、零温差和停机的合法判定依赖已审核规则；未配置规则时保留未确定状态，不能仅凭停机标记生成整段零冷量。

有效零冷量段与缺测段必须可区分。冷量为零不自动抹去同段冷站实际用电；电量边界按所选统计规则独立完整覆盖年度，不通过只保留产冷时段人为提高 EERp。

完整性按每个必需覆盖对象分别验证，再判断年度组合是否完整，不对各测点覆盖率取简单平均。任何未被规则合法覆盖的区间导致年度 `INCOMPLETE`。研发完整样例覆盖完整年度；专业完整性规则未确认不授权现场补齐、外推或降低门槛。

## 8. 年度计算、结果和追溯

自然年度区间为版本化时区中的年初至次年年初，不写死 365 天或全年小时数。底层分段冷量和电量使用相同区间；已有周期结果被精确组合，不能按时长比例猜测分段电量。

```text
EERp_year = sum(Q_c_segment) / sum(E_station_segment)
```

只有完整年度、计量边界完整且分母大于零才生成 EERp。各段比值不做算术平均；分母为零时 EERp 为空，不能输出无穷大。中间计算使用十进制精度，舍入只用于固定版本的结果表达，不改变第 3.3 节比较含义。

结果至少分别记录：

| 分类 | 字段内容 |
| --- | --- |
| 身份与范围 | 结果 ID/修订、建筑、冷站、边界、年度、半开区间与时区 |
| 输入 | 冷量、电量及单位，各分项数量、来源与快照集合 |
| 计算 | 公式版本、介质版本、对齐/积分/舍入规则，未展示舍入的 EERp |
| 状态 | `calculationStatus`、`completeness`、`evaluationStatus`、原因列表及受影响区间 |
| 评价 | 阈值、区间结果、参考来源、规则版本、标准核实状态 |
| 证据 | 关系、测点、质量、计量事件、来源选择、参数与处理标记；实际使用的输入或不可变输入快照 |
| 性质与审计 | 原始数据真实/合成性质、结果 `DEVELOPMENT_SIMULATION`、触发人、时间、traceId、前序结果引用 |

读取真实采样也不能把研发计算结果标为正式，合成样例不能标为真实采样。计算成功与可评价分别判断：规则缺失或不适用时可保存完整计算值，但评价返回具体原因；标准全文未核验本身不阻止已经确认的附件规则产生研发评价。

已完成结果不得原地覆盖。追溯应能够重建本次输入、选择过程和比较结果，不只提供版本号、JSON 标签或哈希。上游原始事实并不保证保留全部历史修正，因此首次计算须固定实际使用的事实快照；若无法取得或保存可复算证据，则标记证据不足，不能声称完整追溯。

## 9. 配置治理、接口和持久化

配置、参数和评价规则使用稳定身份与不可覆盖版本，经过草稿、提交、审核和生效。引用历史版本时检查业务有效期与本次固定版本集合，不用“当前最新”替代历史。维护、审核、计算和重算接入动态后台职责及建筑范围；研发自审仍完整执行两个动作，不能绕过现有职责分离。

拟提供 `/api/v1/energy-efficiency` 下的冷站配置版本、计算规则版本、计算任务、年度结果和追溯查询接口。Controller 只处理入口，业务及事务在 Service；具体 DTO 和错误代码须与上述公共契约同时冻结并生成 OpenAPI。只读结果查询不触发隐式重算，前端不会参与计算。

MySQL 保存配置、版本、审核、计算任务、结果索引和证据清单；TDengine 保存派生制冷功率、周期与年度数值及必要数值证据；Redis 仅作可丢弃缓存。跨数据库不假定分布式事务：先固定任务与证据、幂等写入数值，再确认 MySQL 可见结果；未完成或写失败结果不得冒充当前成功版本。

首版显式发起计算和有界重算，不增加自动依赖调度引擎。同一幂等键和请求摘要返回已有任务，摘要冲突明确拒绝；有界分段任务保存进度，失败可恢复，旧执行不得覆盖新修订。单任务时间跨度、测点数、分段数、单页记录数、超时和并发上限必须有有限配置与测试，超限明确拒绝，不截断后声称完整。

审核、恢复或重算失败保留原因、traceId 和输入身份；修订过期、跨建筑、规则缺失、单位不兼容、来源重叠、锚点不足、时间错配、质量拒绝、零分母、输入不完整、证据不足和资源超限均需稳定错误/业务状态。HTTP 请求错误与合法请求产生的“不可计算/不可评价”结果分开表达。

本设计不预占 Flyway 编号，不创建 SQL 或正式配置种子。编码前按最新主线与并行任务协调迁移；已应用脚本不修改，新增结构和规则不得覆盖电力因子或其他模块数据。研发参数只进入明确的测试配置或经审核的研发版本，不自动激活为生产规则。

## 10. 专业输入清单与研发继续条件

| 待确认输入 | 责任来源 | 缺失时的研发处理 | 正式使用限制 |
| --- | --- | --- | --- |
| 标准第 3 章相关术语、第 4.2 节、第 5.2 节 EERp 公式/阈值表及表注 | 能源专家、正式文本 | 按本文摘录做有来源的研发评价 | 不声明正式标准适用性已验收 |
| 制备端位置、设备覆盖、旁通与共用设备归属 | 能源专家、硬件方 | 使用显式合成拓扑和预期覆盖 | 未确认对象不能正式计算 |
| 水密度、比热、温区、流量计位置及参数适用性 | 能源专家 | 显式测试参数，另测缺失与越界拒绝 | 不采用无来源默认 |
| 测点单位、采样周期、时钟、允许偏差和最长保持时间 | 硬件方、能源专家 | 同步及非同步合成样例，阈值由测试明确传入 | 不承诺现场同步与精度 |
| 积分方式、最大间隔、完整性与零值/停机规则 | 能源专家 | 完整样例及拒绝样例，配置留版本 | 不放宽质量或推算缺口 |
| 实际输入与期望冷量、电量、EERp | 能源专家、现场团队 | 用确定性合成样例完成软件验证 | 另行专业及现场验收 |

这些缺失项不阻止设计确认后进行配置框架、确定性算法、研发规则和拒绝路径的实现；它们阻止的是未经确认的现场专业判断和正式评价。不得把附件中“开发必须”等措辞自动扩展为用户未批准的功能。

## 11. 验收矩阵

以下是已确认的验收矩阵；实际自动化入口和仍未完成的验证层级见第 13 节。

| 编号 | 场景 | 期望证据 |
| --- | --- | --- |
| A01 | 合成完整年度冷量 5,000,000 kWh、电量 1,000,000 kWh | EERp=5，研发规则结果 GUIDANCE_ONLY；年度完整性由完整快照证明 |
| A02 | 冷量表正常差值、复位、回绕、换表 | 复用上游算法，事件、锚点和版本可追溯 |
| A03 | 水参数 rho=1000、cp=4.18，流量100 m3/h、供7℃/回12℃保持1小时 | 显式合成样例约580.555556 kW、580.555556 kWh；不表示该短样例可评价全年 |
| A04 | 流量和温差共同变化 | 对齐后乘积与积分符合手算，拒绝平均值相乘替代 |
| A05 | 时差、保持时长、积分间隔在限制内/等于限制/超限 | 端点归属明确，拒绝段不被好点跨越，处理标记保留 |
| A06 | 合法零值、规则缺失、负流量、反向温差、介质越界 | 零与缺测分开，非法输入不取绝对值、不补零 |
| A07 | 总分表重叠、不同水路、共用设备未分配 | 稳定拒绝并指出覆盖对象 |
| A08 | 来源、关系、参数在年内切换 | 精确分段，缺口/重叠拒绝，历史结果不改写 |
| A09 | 年度未结束、缺必需机组、热量/电量周期错配、零分母 | 不生成完整年度 EERp；已有数量与原因可查询 |
| A10 | 年初年末、闰年、时区、分段端点、亚秒时长 | 无重复、无静默截断；周期算法复用且精度可验证 |
| A11 | 3.99、4、4.5、5、5.01及显示舍入跨阈值 | 与第3.3节一致，典型工况阈值不混入全年 |
| A12 | Q0默认、显式允许/拒绝Q1/Q2、空集合、策略不可用 | 按场景执行；测试许可不是现场专业批准 |
| A13 | 参数缺失、草稿未审、有效期冲突、越权、职责撤销 | 失败关闭，审核和审计完整 |
| A14 | 相同/冲突幂等请求、并发修订、写入失败、重试与进程恢复 | 无重复当前结果，失败不提前可见，旧执行不覆盖新版本 |
| A15 | 使用保存输入按固定版本复算 | 数量、EERp、比较和来源选择可重建；不仅检查哈希存在 |
| A16 | 两种来源经公共业务契约进入原生量周期再生成EERp | 不以直接给核心塞总量代替链路验证，既有tce和碳结果不受影响 |
| A17 | 达到/超过配置容量上限 | 有界分页、任务、超时和明确拒绝，无截断后完整声明 |

验证层级：定向公式/契约测试 → 后端普通回归 → 隔离 MySQL/TDengine 迁移与链路验证 → 专业样例 → 现场验收。普通测试不得连接现场或生产资源；隔离集成须显式 opt-in，合成输入与目标引擎运行分别报告。按 [iot-change-verification](../../.agents/skills/iot-change-verification/SKILL.md) 和 [仓库流程](../development/repository-guardrails.md) 执行对应检查，CI 必需检查不绕过。

## 12. 实施顺序与完成条件

1. **设计审阅及公共契约冻结**：确认本文；核对最新主线、上游只读输入和原生量周期扩展；在代码变更前完成有限资源上限、DTO/状态、持久化及参数策略的技术细化。必要依赖若与既有硬约束冲突，先处理设计差异，不静默替代。
2. **配置与公共输入**：实现冷站覆盖配置、版本审核、公共测点读取及来源证据，先验证拒绝和权限，再进入依赖计算。
3. **两条冷量链与周期接入**：累计量复用；温差功率、时间对齐和积分适配；原生量周期、版本与不可变证据。此步未通过公共链路验证，不进入年度完成声明。
4. **年度EERp及有限评价**：接入电量边界、完整性、严格阈值比较、结果和追溯接口，完成显式有界重算和失败恢复。
5. **验收及Git交付**：运行范围匹配验证，更新当前状态和实际接口入口，提交、推送并通过PR交付。公共契约、迁移和文档按最新主线集成，不覆盖并行任务内容。

软件完成条件为A01–A17对应证据及仓库必需检查齐备，且结果仍按证据保持研发候选性质。专业与现场待确认项集中保留，不将计划、模拟测试或设计文档合并描述为指标已经实现。业务页面及后续指标由单独范围确认驱动。

初始设计文档交付只检查链接、术语、规则一致性和差异；编码交付按生产代码验证矩阵执行。编码前执行任务预检；涉及生产代码时使用 code-comment-quality，文件变更与交付分别遵守 iot-change-verification 和 safe-pr-delivery。无需另建重复任务计划或通用架构文档。

## 13. 实际后端契约与验证边界

### 13.1 模块及持久化

- `iot.calculation.CalculationPointReadService` 提供计算场景公共读取。读取包含结束时刻锚点，实际积分区间仍为 `[from,to)`；累计表计事件按读数转换 `(previous,current]` 归属，原生入口的事件查询为 `(from,to]`，不改变旧聚合入口。
- 当前资产档案的 `ANALOG/ACCUMULATE` 是采样形态。冷量、电量累计表要求 `ACCUMULATE+kWh`；流量要求 `ANALOG+m³/h` 或 `m3/h`，温度要求 `ANALOG+℃` 或 `degC`。等价符号规范化固定为 `EERP_CANONICAL_ALIASES_V1`，未实现任意单位转换。
- 配置创建、审核和生效核对当前冷站台账、历史 `MEASURES` 边及电表分配/层级，并固定引用证据。冷站台账中的冷机、泵、塔须完整覆盖，空调末端不进入分母。专业审核负责确认实际水路、安装侧及制备端位置；软件的配置校验不能替代现场测量位置核实。
- 冷量组装调用 `NativeQuantityAggregationService`，累计表复用既有修正及事件算法，功率段复用原生阶梯积分。保持到期和每路 `maxGap` 到期均为断点，任一路新样本不能延长其他两路有效期。
- `NativePeriodSnapshotService` 提供独立原生量发布/读取，不读取 tce 规则。V42 新增冷站配置、任务、并发互斥及原生量索引表；TDengine 复用既有周期数值存储。兼容数值表中的 `point_id` 以 `NATIVE:` 前缀表示派生序列身份，实际物理点依赖保存在不可变输入证据中。
- MySQL 先固定任务 `stage_json`、实际输入、规则和数字摘要，再幂等写 TDengine，全部写入成功后任务才为 `SUCCEEDED`。恢复已有 stage 时不重新读取原始点。原生量数值的 `tce` 列为空；覆盖率的 TDengine 显示副本使用 18 位小数以符合既有列宽，完整精度和完整性判断仍在 MySQL 固定证据中。
- 没有任何有效冷量/电量时对应总量为 `null`，有效零值为 `0`。不完整周期可保存和封账，但年度不会把它当作完整输入。年度只从显式 `SEALED` 周期集合读取，不扫描全年原始点；跨配置切换须提交精确分段，不能跨生效边界计算或比例拆量。

### 13.2 API 与动作

部署默认上下文为 `/api`，以下列出 Controller 的 `/v1/energy-efficiency` 路径。请求与响应以 `EerpContracts` 及生成的 `/v3/api-docs` 为准。

| 方法和子路径 | 行为 |
| --- | --- |
| `POST /configurations` | 创建不可覆盖的配置版本草稿；映射、物性、时间规则和研发评价引用同版审核 |
| `GET /configurations`、`GET /configurations/{id}` | 按建筑/冷站有限列表或明确版本读取 |
| `POST /configurations/{id}/actions/{action}` | `SUBMIT`、`APPROVE`、`REJECT`、`ACTIVATE`，携带 `expectedRevision` 和理由 |
| `POST /period-tasks` | `idempotencyKey/configVersionId/fromInclusive/toExclusive/asOf/predecessorTaskId`，计算一个有界原生量周期 |
| `POST /annual-tasks` | 建筑、冷站、年份、时区版本和显式 `periodTaskIds`，汇总完整自然年度并作研发评价 |
| `POST /tasks/{id}/actions/{action}` | `APPROVE_RECALC`、`SUBMIT_SEAL`、`APPROVE_SEAL`，动态职责和职责分离在后端执行 |
| `POST /tasks/{id}/resume` | 恢复失败或租约过期的任务；已完成任务返回原结果 |
| `GET /tasks`、`GET /tasks/{id}`、`GET /tasks/{id}/trace` | 有限任务列表、结果及实际输入证据；只读操作不触发重算 |

配置状态为 `DRAFT → SUBMITTED → APPROVED → ACTIVE`，驳回进入 `REJECTED`。已生效区间不可原地改写；来源/参数切换以不重叠的明确生效段建版本。任务一般为 `READY → RUNNING → SUCCEEDED → PENDING_SEAL → SEALED`；失败进入 `FAILED`，恢复可重新领取租约。带前序任务的重算先进入 `PENDING_RECALC`，经另一审核动作批准后为 `READY`，原结果继续保留。结果中的任务执行状态、周期完整性和年度可评价状态分别表达。

### 13.3 有限资源与使用限制

`energy.eerp` 配置提供：周期跨度默认/硬上限 86,400 秒、测点数默认/硬上限 32、年度周期数默认 1,024/硬上限 5,000、并发默认 2/硬上限 4、执行预算默认 45 秒/硬上限 60 秒、单任务证据默认 8 MiB/硬上限 16 MiB。单路保持、时差和最大积分间隔最多一天；每次原始读取固定 500 条一页、20,000 条事实硬上限。每测点计量事件和修正各限 1,000 条，在 SQL 读取阶段取上限加一以拒绝溢出。配置引用的建筑资产/关系读取各限 5,000 行，节点关系单页上限 100，超限拒绝。

执行预算在依赖调用前后和分段之间检查，不强制杀死正在执行的数据库调用；数据库互斥、执行租约和本机槽位同时限制运行，旧租约不能完成新执行。依赖调用长期阻塞时不会继续无限增加本机执行线程。尚需隔离目标引擎及部署容量验证，不能由合成测试推定生产长期吞吐。

### 13.4 本候选的验收证据

本次最终 `./mvnw.cmd --batch-mode --no-transfer-progress verify` 已通过：922 项测试，0 失败、0 错误，其中 40 项条件集成测试跳过（包含本模块尚未启用的目标引擎测试）。该命令同时完成后端编译、普通回归与打包；不把跳过项计为通过。

- `CalculationPointReadServiceTest`：实际公共读取服务的端点锚点、水位、排序、质量阻断、来源性质未知、32 点和 20,000 事实边界。
- `CoolingComputationCoreTest`、`NativeQuantityAggregationServiceTest`：水物性/对齐/积分、逐路失效、有效零值与非法值、毫秒精度、严格累计锚点、末端复位、回绕/换表/修正及治理证据预算。
- `EerpServiceTest`：实际公共读取与算法、H2 配置/任务/快照索引、两条冷量来源、365 个封账周期的年度组合、来源切换、配置覆盖、动态职责调用、审核、幂等冲突、写失败恢复、旧租约拒绝及审计回滚。TDengine 在本层使用明确的数值存储替身，实际输入是确定性合成原始点。
- `EerpAnnualCoreTest`：完整年度 5,000,000/1,000,000=5、闰年、纽约 DST、缺口/重叠、不完整输入、零分母、4/5 严格边界及显示舍入跨阈值。
- `EerpApiContractTest`：真实 Spring HTTP 鉴权、动态职责、非法 JSON、错误码和生成 OpenAPI；普通测试隔离外部资源。
- `EerpMysqlTdengineIntegrationTest`：已提供显式启用入口，**尚未执行目标引擎验证**。需 `EERP_IT_ENABLED=true`、`EERP_IT_ISOLATED=true`，并由隔离环境提供 `EERP_IT_MYSQL_URL/USER/PASSWORD` 与 `EERP_IT_TDENGINE_URL/USER/PASSWORD`。MySQL 必须是一次性空库，数据库名按现有迁移链为 `iot_platform`；测试随机创建并回收专用 TDengine 库，不接受现场或生产资源。

目标引擎测试命令为 `./mvnw.cmd -Dtest=EerpMysqlTdengineIntegrationTest test`。已提供的自动化入口不等于该层已通过；本候选仍缺目标引擎联合证据、干净任务独立模块验收、专业规则和现场验收，不声明正式标准符合或软件验收全部完成。
