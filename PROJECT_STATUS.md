# 建筑能碳监测管理平台项目状态

## 1. 状态口径

本文描述当前 Git 版本，不描述某台电脑的瞬时运行状态。

- **已实现**：当前代码存在且有相应自动化或既有验收证据；
- **部分完成**：只有旧系统能力、单侧实现或有限场景验证；
- **计划中**：已确认方向，但尚未形成完整代码和验收证据；
- **暂缓/不做**：当前版本不安排，不能作为隐含需求开发。

合成测试、真实设备连接、现场验收和标准符合性是不同证据层级，不得互相替代。

## 2. 当前权威基线与活动候选

### 2.1 当前权威基线

- 代码来源：旧中央空调仓库 `iot-platform-demo`；
- 冻结标签：`baseline/hvac-before-energy-carbon-20260823`；
- 定位：可复用的中央空调采集、质量、指标、查询和展示基础；
- 边界：不是建筑能碳平台正式版本，不证明 GB/T 47474—2026 整体符合，也不证明所有现场条件已验收。

### 2.2 活动候选

- 产品名称：建筑能碳监测管理平台；
- 开发仓库：`building-energy-carbon-platform`；
- 当前状态：已建立新旧系统边界和阶段方向，首个建筑能碳后台业务闭环已形成活动候选实现，但尚未成为经批准的正式版本；
- 已确认候选设计：[`数据源与采集策略治理闭环设计`](docs/designs/2026-08-24-collection-source-policy-governance-design.md)，对应候选代码已实现并通过隔离自动化验证，MySQL 8 迁移已随 Flyway 完整链成功执行；治理执行语义、外部联调和现场验收仍未独立验证；
- 已确认候选设计：[`能源采集元数据完善设计`](docs/designs/2026-08-31-energy-collection-metadata-design.md)，对应后端活动候选已实现独立能源测点属性、建筑范围、乐观锁、采集域审计、专业选项和采集上下文查询，并通过 H2 服务、事务回滚、API 与迁移契约隔离验证；V31 已随隔离 MySQL 8 空库 V01-V32 完整迁移链执行并通过专业枚举与复合外键约束验证，能源专家确认、真实现场数据和独立验收尚未完成；
- 已确认候选设计：[`Q0/Q1/Q2 使用策略治理与执行闭环设计`](docs/designs/2026-08-24-quality-usage-policy-governance-design.md)，对应完整后端候选代码已实现并通过隔离自动化验证，MySQL 8 迁移已随 Flyway 完整链成功执行；TDengine 3.2.3 执行语义与查询计划、外部联调和独立验收仍未验证；
- 已实现多能源活动数据只读候选：新增 `/v1/energy-activity-data/raw-events`，按建筑、测点集合、半开时间区间和 seek 游标有界读取 TDengine 原始事件；只有具备已确认能源专业属性且通过 `ENERGY_ACTIVITY_AGGREGATION` 质量门禁的数据才返回，默认无正式策略时仅允许 Q0，并在响应中保留能源属性修订和质量策略版本证据。当前原始事实不保留重复次数及修正历史，接口已显式声明该证据限制；Mock、H2 API、OpenAPI 与迁移契约隔离测试已通过，V33 已随隔离 MySQL 8.4 空库 V01–V38 完整迁移链执行，TDengine 3.2.3 已验证模拟多测点同时间排序、seek 分页、起始锚点和查询计划；读取边界使用毫秒时间戳以避免应用与数据库时区偏移，真实能源测点和现场数据尚未验证；
- 已确认候选设计：[`第七闭环：能源计量、折标与时段聚合设计`](docs/designs/2026-09-01-energy-metering-standard-coal-aggregation-design.md)，已冻结固定源 11 种化石燃料、电力和外购热力范围，以及能源品种、单位量纲、三类活动语义、计量事件、折标煤、质量完整性、计量边界汇总、月度封账、例外和增量重算边界；五个后端切片已完成后端软件收口候选：能源字典与测点绑定、折标参数与确定性研发模拟 `tce`、三类活动量聚合及真实输入适配、周期当前投影与月度封账及有界重算、计量边界汇总与多维查询；研发模拟全链路已覆盖版本审核、活动聚合、折标、月度封账、历史关系汇总、迟到修正和同规则重算，V34–V38 已随隔离 MySQL 8.4 空库完整迁移，TDengine 3.2.3 已验证原始活动读取及周期数值幂等更新。历史 Excel 与首批种子全部保持待专业确认、生产不可用，移动源和规则缺失/冲突安全拒绝；该结论是软件收口候选，不等于正式生产结果、专业验收、真实设备/建筑、现场验收、碳排放或可视化完成；
- 已确认候选设计：[`碳管理基础闭环设计`](docs/designs/2026-09-02-carbon-management-foundation-design.md)，已有因子/分母版本、范围一/二计算、月季年排放、年度唯一两项强度、短事务、持久化重算和批次审批代码，计算只消费第七闭环封账汇总。当前状态为**部分完成，软件验收不通过**：本次真实 MySQL、HTTP 并发与 JVM 重启验收取得正常路径和容量证据，但复现总超时、同键恢复、影响分析滞留、重算租约/恢复、冻结后归并、失败隔离及分母冲突等阻断问题，见[软件验收报告](docs/reviews/carbon/2026-09-03-software-acceptance.md)。V39 空库迁移及无业务种子验证通过；专业因子、真实分母/建筑、生产长期容量和现场验收仍未完成，不能将已合并代码视为完整软件闭环；
- 已确认候选设计：[`MQTT TLS 与遥测补传可靠性 V2 设计`](docs/designs/2026-08-25-mqtt-tls-telemetry-reliability-v2-design.md)，平台和适配器候选代码已实现并通过隔离自动化验证；正常首报已收敛为一次 MySQL 终态回执写入，成功 ACK 只进入监控，成功回执默认保留 24 小时、异常明细保留 180 天并持续清理；临时证书、隔离 EMQX、TLS 主机名校验、Paho QoS 1 往返，以及隔离 MySQL 8/TDengine 3.2.3 双存储和应用 ACK 已完成软件联调，生产保留期、生产 Broker/证书、真实网络和网关缓存仍未验证；
- 已确认候选设计：[`标准设备参数读取与参数版本治理闭环设计`](docs/designs/2026-08-25-device-parameter-governance-design.md)，对应后端活动候选已实现并通过 H2、Mock 和 Stub 隔离自动化验证，MySQL 8 迁移已随 Flyway 完整链成功执行；TDengine 3.2.3 执行语义、厂家报文、真实 Excel、历史生产数据迁移、外部联调和独立验收仍未验证；
- 已确认候选设计：[`空间—系统—设备—测点—计量边界关系治理闭环设计`](docs/designs/2026-08-26-space-semantic-metering-relation-governance-design.md)，对应后端活动候选已实现建筑级原子版本、物理与语义分层、表计总分层级与计量方向、显式校验、审核/生效/回滚/审计、研发自审例外与生产启动门禁、旧入口保护、计量未分配状态和分层查询，并补齐平台标准 V1 Excel 模板下载、无写预检和幂等原子草稿导入；H2、API 契约、Excel 模板渲染及隔离 MySQL 8 空库 V01-V32 完整迁移和生效事务验证已通过。数值化性能、独立验收、能源专家真实表格适配、专业关系确认和真实计量图映射仍未完成，前端页面、计量分摊、动态碳因子与园区级跨建筑关系不在本候选范围；
- 已确认候选设计：[`后台职责分离与统一审计证据治理设计`](docs/designs/2026-08-26-backoffice-duty-audit-governance-design.md)，已确认保留四类业务角色、独立后台职责、关闭普通直接发布、研发完整两步自审、生产双人审批、公共审计字段与联邦查询、默认脱敏导出、保留策略审核和到期自动清理边界；公共基础候选已实现固定职责目录、动态职责校验、生产自审启动门禁、服务端追踪、安全拒绝和登录事件、通用敏感申请状态机，以及职责、账号开通/停用/恢复、正式角色、建筑范围、菜单定义和角色菜单白名单处理器，对应角色、建筑、菜单和后台账号敏感旧直改入口已稳定拒绝；账号开通申请包一次审核并在一个事务中创建账号、角色和建筑授权，初始密码及重置密码不进入申请或审计，执行后只返回一次性令牌且 MySQL 仅保存 SHA-256；既有建筑访问申请保留原审核状态机，只增加审核职责和指向原申请编号的最小公共证据；采集治理和质量使用策略均已保留原领域审核状态机并接入公共提交/审核职责与研发/生产自审策略，普通直接发布入口稳定拒绝；系统安全、采集、质量使用、设备参数、设备接入和关系治理六类来源已通过适配器提供统一查询，按当前后台职责和建筑范围授权，异步 CSV 导出要求独立导出职责并固定默认脱敏，不集中复制领域完整事实；保留策略新增、修改和启停复用公共敏感审批并按自然月/自然年创建不可改写版本，保全设置要求专项职责且解除必须审核，正常到期批次自动执行，提前、强制和手工范围删除审核后异步分批执行，保全、未决调查和未完成审批均在 SQL 删除前排除，清理批次长期保存范围、数量、结果、`traceId` 和校验摘要而不复制已删内容。上述能力已通过 H2、API 和一次性隔离 MySQL 8.4 空库 V01-V30 完整迁移及分批删除验证；每日 10 万条合成容量基线已验证第 100001 条关键事件不丢弃、六来源联邦查询和保留清理使用索引且无文件排序，隔离环境新增分配约 621 B/条，500 条事务批次共 200 批清理 99,995 条并保留 6 条保全证据。该结果不是生产 SLA；历史记录缺失的公共字段保持空值而不伪造，生产数据库迁移、生产人员分工、适用制度、备份周期、实际磁盘容量、安全验收和独立候选验收仍未完成；
- 批准条件：阶段功能完成验证、外部专业输入确认、必要的硬件/现场验收完成，并由用户最终批准。

## 3. 继承能力

以下能力来自中央空调基线，可继续复用，但不能扩大描述为通用能碳能力：

| 能力 | 当前状态 | 边界 |
|---|---|---|
| Spring Boot、Vue、Java 21 与仓库质量防护 | 已实现 | 仍需按新模块运行本次验证 |
| MySQL、TDengine、Redis、MQTT、WebSocket 基础设施 | 已实现 | 外部资源是否可用需在目标环境核验 |
| 登录、角色、菜单和建筑范围权限 | 已实现基础 | 新模块仍需逐项接入后端权限 |
| 建筑、空间树、系统、设备、测点档案 | 已实现基础；关系治理为活动候选 | 语义关系和计量边界只有软件治理闭环，不代表专业映射或现场验收 |
| 能源测点专业属性 | 已实现后端活动候选 | 只治理能源类型、电力来源、数据性质、统计周期和确认依据；不含单位换算、聚合、碳计算或现场专家确认 |
| 标准 MQTT 报文、设备身份、未知设备有界发现与绑定 | 已实现基础 | 不等于 Modbus、BACnet、NB-IoT 等多协议已经接入 |
| MQTT TLS、V2 消息幂等、平台持久化回执与 ACK 分级 | 已实现活动候选 | 已完成软件隔离验证；不等于生产 Broker、真实网络、设备直收 ACK 或网关缓存验收 |
| 原始时序、分钟聚合和 Q0/Q1/Q2 处理 | 已实现 HVAC 基础 | 质量等级适用范围及规则接口仍需按指标确认 |
| HVAC 19 测点、四项公式、查询、实时卡片和历史趋势 | 已实现继承场景 | 不能直接视为多能源、碳计量或完整能效评价 |
| 合成设备接入链路 | 已验证继承场景 | 不等于真实硬件、TLS、网络抖动和长期现场验收 |
| 真实设备 MQTT 连接 | 已有既有连接验证记录 | 只证明对应设备与当时链路可用，不代表新平台和多协议全面验收 |

主要证据入口：[`com.platform.iot`](src/main/java/com/platform/iot)、[`com.platform.hvac`](src/main/java/com/platform/hvac)、[`web/src`](web/src)、[`PROJECT_GUIDE.md`](PROJECT_GUIDE.md)。

## 4. 产品阶段

### 阶段一：数据监测与后台基础架构

目标：把多来源数据转为可识别、可校验、可追溯、可查询的平台事实。

当前状态：**部分完成**。

- 复用现有 MQTT、资产、权限、时序、聚合和质量基础；
- 已实现数据源与采集策略治理活动候选：包含数据源、来源别名、策略版本、审核、审计、建筑范围、并发修订、原子首启、升级/回滚、运行快照与刷新状态；
- 已实现能源采集元数据后端活动候选：独立保存能源类型、电力来源、数据性质、月度统计与年度汇总要求，单位和用能系统继续引用既有权威数据；支持专业待确认、修订冲突、建筑越权、采集域审计和“数据源—现场点码—标准测点—能源属性—采集策略”只读上下文，且不改变设备上报周期、采集策略修订或运行快照；继承 19 点未被自动补造专业属性；
- 已将继承的 19 个 `MQTT_FREEZE_V1` 测点纳入初始数据源和有效策略迁移，并让既有设备接入别名关联唯一可确定的启用数据源；
- 管理 API 已实现于后端 `/v1` 契约；本候选未开发 Vue 页面；
- 扩展冷热源、空调及通风、供配电和环境监测的数据目录及接入配置；
- 已实现标准设备参数读取与参数版本治理后端活动候选：覆盖 `DEVICE/MANUAL/EXCEL/TEMPLATE` 来源、标准定义与适用关系、映射版本、候选与冲突、整组双时间版本、职责分离审核、立即/受控追溯生效、差异、平台侧回退、双口径查询、公式版本证据、历史重算任务和遗留参数无损暂存；不包含参数下发，未修改旧 Vue 页面；
- 已实现按测点、场景治理 Q0/Q1/Q2 使用范围的活动候选：包含版本、变更集、审核、审计、全局修订、运行快照、实时/历史/公式门禁、TDengine 尝试事实与当前状态投影，以及有界纠正、恢复和对账；
- 已实现多能源原始活动数据读取后端候选：下游不再复用 HVAC 查询 DTO 或直接读取上游内部表；请求必须通过建筑范围、已确认能源属性和 `ENERGY_ACTIVITY_AGGREGATION` 质量门禁，支持最多 100 个测点、31 天半开区间和稳定 seek 分页，返回原始单位、事件/入库时间、迟到标记、能源属性修订与质量策略证据；无策略时默认只允许 Q0，被拒绝行不进入业务数据；
- 已实现第七闭环第一后端切片活动候选：粗能源分类继续留在采集元数据，权威计算分类独立使用能源品种；首批能源品种和单位按稳定身份与不可覆盖版本保存，兼容矩阵不预置专业事实，测点原始单位继续只读 `biz_data_point.unit`；维护和审核分别要求动态职责，所有新增版本先待专业确认，测点绑定还必须通过已确认专业属性、已审核品种/单位/兼容规则、粗分类一致和建筑范围校验；移动源、`kgce/tce` 原始活动单位、未知固定换算和缺失规则均安全拒绝，未开发 Vue 页面；
- 已实现第七闭环第二后端切片活动候选：标准煤低位热值、折标公式和折标参数按稳定身份与不可覆盖版本治理，维护、审核和模拟执行使用独立动态职责；公式硬性区分直接折标、低位热值和能源当量算法，以及热值当量和一次能源等价口径；固定比例单位通过能源字典公共只读契约换算，计算使用 `DECIMAL128` 中间精度并保存能源品种、输入/目标单位、公式、参数和标准煤版本证据；HTTP 仅输出明确标记的研发模拟结果，移动源、草稿参数、失效引用、量纲不兼容、规则缺失或冲突均安全拒绝，不包含正式活动量计算、舍入、封账、碳排放或 Vue 页面；
- 已实现第七闭环第三后端切片活动候选：`EnergyAggregationInputPort` 固定活动水位、计量关系、测点绑定、质量策略、计量事件、修正和积分策略版本，聚合核心不读取上游数据库或 Mapper；真实输入适配器通过能源活动数据、品种绑定和有效计量分配的公共服务读取事实，固定计算水位并装配累计量起始锚点与区间末边界；复位、回绕、换表和数据错误事件、活动修正及瞬时积分策略均按稳定身份与不可覆盖版本持久化，并接入建筑范围、动态职责分离、审核、质量再校验和审计；周期量因上游尚不提供源周期起止证据而安全拒绝，HTTP 只输出明确标记的研发模拟结果。正式结果、周期当前投影、封账/重算、计量边界汇总、碳排放和 Vue 页面仍未实现；
- 已实现第七闭环第四后端切片活动候选：版本化时区按自然日/月/年生成半开周期，开放期按建筑、测点和周期仅维护一条带修订号的当前投影；月度结果达到延迟窗口后仍须提交与职责分离审核，例外策略按问题代码、阈值、证据和版本决定阻断、带例外封账、排除受影响项或仅封原生量，未知问题继续安全拒绝；封账快照不可覆盖，迟到和修正按周期合并脏标记，重算以最多 100 项批次执行并区分同规则复算与历史重述，只追加发生实质变化的快照，未完成或失败批次结果不对查询可见；MySQL 保存治理、索引和批次状态，TDengine 保存数值副本及精确十进制文本，HTTP 仅输出研发模拟结果，未开发 Vue 页面；
- 已实现第七闭环第五后端切片活动候选：计量边界按版本化研发口径显式选择总表、分表、独立表或总表带分表明细，总分表不得直接相加；查询只消费可见封账快照，并按快照证据读取对应历史关系版本，不用当前关系静默改写历史；空间、系统和设备归属不自动分摊，多对象无审核规则时进入未分配桶，总表与分表差额单列残差；购入、外送和净值分别保留，原始量按单位分开，`tce` 只按相同口径汇总；接口支持白名单维度和有界结果集，并返回覆盖率、完整性、锁定状态、关系/折标/汇总口径版本及证据哈希。V38 和现有模拟策略只支持 `DEVELOPMENT_SIMULATION`，未开发 Vue 页面、碳排放、自动分摊或移动源；
- 碳管理基础后端部分完成：正常核算、分母缺失、幂等/竞争请求、快照与明细上限、冻结前归并、候选审批、发布事务回滚及 `DEAD` 显式恢复已有隔离目标引擎证据。仍存在超时只能事后发现、同幂等键不能恢复过期批次、多个重启窗口丢失可执行状态、过期工作线程可改写新租约项、冻结批次追加新变化、成功项被失败重试拖住审批、后续模拟变化漏算及分母冲突阻断总量等问题。现有版本 ID 和哈希不等于设计要求的完整追溯已验收；未开发 Vue 页面，未预置业务因子和建筑分母；
- 已实现 MQTT TLS 与平台补传可靠性活动候选：包含外部 truststore/keystore、强制主机名校验、连接故障分类及聚合留痕、V2 缺字段自适应、消息级去重/冲突、不可覆盖 TDengine 写入、单次 MySQL 终态回执、成功 ACK 监控和失败明细、设备直返/适配器代理/仅证据三级 ACK，以及受建筑权限保护的 24 小时热窗口只读查询 API；
- 已用 Flyway 统一 MySQL 新库初始化和增量版本链；Docker Compose 不再并行执行 SQL。非空旧库默认拒绝自动接管，仅允许在备份、结构核验和隔离演练后一次性 baseline；
- V1 旧单点和标准 Topic 继续兼容，适配器内部 V2 Topic 由 ACL 边界隔离；本候选未实现网关本地持久化缓存、容量/淘汰/断电保留、补传限速、真实断网恢复和设备控制；
- 已为三个初始场景和继承的 19 个标准测点提供 57 个初始保守策略迁移；本候选未开发 Vue 页面，也未修改质量生成算法、公式和专业阈值；
- 已实现建筑级关系治理后端活动候选：受控初始化只复制明确旧外键，提供单草稿、乐观锁、完整图校验、提交快照、职责分离审核、显式立即生效、新草稿回滚、追加审计、旧投影原子更新、旧写入口旁路保护和有界分层查询；当前有效计量分配查询组合生效版本中的测点、计量边界、表计结构、目标空间/系统/设备和关系版本证据，并显式保留未分配项；表计结构独立保存 `MAIN/SUB/INDEPENDENT/UNKNOWN`、直接上级和 `INBOUND/OUTBOUND/BIDIRECTIONAL/UNKNOWN`，并参与版本差异、审核、生效、回滚、审计和查询；平台标准 V1 Excel 支持模板下载、无写预检及携带修订号和幂等键的原子草稿导入，不自动提交、审核或生效；研发可显式启用同账号提交后审核，生产错误配置会拒绝启动；本候选未开发 Vue 页面；
- 已实现后台职责与审计治理公共基础候选：职责不写入 JWT，按 MySQL 当前事实动态判定；研发自审必须完整调用提交、批准和执行，生产环境误开自审会拒绝启动；系统敏感命令只允许代码注册的白名单处理器，已接入职责授予/撤销、账号开通/停用/恢复、正式角色、建筑范围、菜单定义和角色菜单，角色、建筑、菜单和后台账号敏感旧直改入口已拒绝；账号开通一次审核并原子写入账号、角色和建筑授权，初始密码和重置密码通过默认 15 分钟、仅存 SHA-256 的一次性令牌交付；建筑访问申请继续由原领域记录保存完整事实，公共层只补审核职责和最小证据；所有 HTTP 错误附带服务端 `traceId`，登录、Token 撤销、角色或职责拒绝和敏感申请状态变化写入系统安全审计；六类权威来源已通过联邦适配器提供统一检索，异步 CSV 导出要求查看和导出两项动态职责、沿用同一建筑范围并固定默认脱敏；保留策略、证据保全、审核后例外删除、正常到期自动清理和长期清理批次证据已形成候选实现，研发自动到期清理默认关闭；
- 采集治理已关闭数据源、别名和策略版本的普通直接发布：首次启用、正式停启和策略停用统一进入原采集领域审核状态机，只复用公共提交/审核职责和研发/生产自审策略，不向公共申请表复制领域草稿；旧草稿启用和版本直发入口保留稳定拒绝，运行快照仅在审核通过后刷新；
- 质量使用策略已删除新增 `DIRECT_PUBLISH` 的代码路径：正式发布统一进入原质量领域审核状态机，只复用公共提交/审核职责和研发/生产自审策略；旧直接发布 URL 保留稳定拒绝，历史直接发布证据保持原值，研发自审仍完整执行提交和批准两个动作；
- 继续由能源专家确认首批能源测点属性、单位适用性和验收数据；软件不补造缺失专业事实。

阶段一完成不等于能效评价或碳管理已经完成。

### 阶段二：可视化实现

目标：在可信数据和专业规则基础上形成可解释的监测、能耗和碳排展示。

当前状态：**计划中；已有 HVAC 页面可复用**。

- 核心看板：实时功率、今日累计能耗、今日碳排和能效排名；
- 建筑 → 区域 → 设备三级下钻及多维对比；
- 设备状态、功率/COP、历史趋势和质量状态；
- 能流图/桑基图和损失节点；
- 碳排总览、设备排行、区域碳强度、24 小时时段分布及能耗/碳排对比。

能源指标、折算、碳因子、能流和展示判定由能源专家确认；前端不自行生成结果。

### 阶段三：虚拟数据测试及次级应用点完善

目标：使用可追溯虚拟数据验证新链路、异常路径和次级应用，再决定真实现场扩展。

当前状态：**计划中**。

- 建立覆盖正常、缺失、重复、乱序、越界和设备离线的虚拟数据集；
- 验证接入 → 预处理 → 存储 → 计算 → 查询 → 展示全链路；
- 验证质量策略切换、关系映射、空间分摊、权限和审计；
- 实现或完善实时告警规则、事件、处理记录、统计、日志检索和次级查询；外部消息推送、工单和跨系统联动暂缓；
- 虚拟验收通过后，另行开展真实设备、多协议和现场稳定性验收。

## 5. 外部输入和依赖

| 来源 | 当前必须提供的输入 |
|---|---|
| 能源专家 | 指标定义、标准设备参数语义及适用关系、单位/折算、精度与边界、公式参数依赖、碳因子、计量边界、Q 策略适用范围、阈值、基准、诊断与归因规则、展示逻辑、验收样例 |
| 硬件人员 | 设备清单、设备参数报文、协议、字段路径、单位、时间语义、采样周期、配置版本、边缘缓存/补传、设备能力和现场联调条件 |
| 软件开发 | 数据模型、配置版本、接口、权限、存储、任务、日志、测试、可视化和可追溯实现 |

缺少专业输入时，软件可以建设配置框架、安全默认和拒绝路径，但不能自行补造规则。

## 6. 当前暂缓或不做

- 远程参数配置、升级、监控、设备启停、模式切换和故障恢复命令；
- 边缘侧诊断、控制及由软件单独承担的边缘硬件能力；
- 响应与执行；
- 负荷预测和能源审计；
- 策略生成管理和策略下发管理；
- 运行模式管理及完整自动运行报告；
- 告警短信/邮件推送、自动工单、应急处置、跨系统复合告警；
- 日志聚类和异常行为分析；
- 设备拓扑与空间模型叠加、BIM 热力图；
- 未经确认的其他建筑设备系统。

## 7. 风险与未验证项

- 新仓库仍保留 `hvac` 包名、19 测点文档、旧脚本和制品名称；这是继承代码，不是新产品边界，按实际迁移逐项处理。
- 当前只具备 MQTT 基础链；多协议需要硬件、网关/驱动、统一契约和大量真实测试共同完成。
- 数据源与采集策略治理的 MySQL 8 结构迁移已随 Flyway 完整链成功执行；治理读写语义仍只在 H2、Mock 和 Stub 隔离环境验证，运行快照仍是当前单体实例内存状态，不含多实例广播。
- 能源采集元数据已形成后端活动候选并通过 H2 服务、事务回滚、API 和迁移契约隔离验证；V31 已随隔离 MySQL 8 空库 V01-V32 完整迁移链执行，并通过专业枚举与复合外键约束验证。继承 19 点保持无专业属性，不代表能源专家已确认或真实能源采集已完成。
- Q0/Q1/Q2 使用策略治理已形成后端活动候选并通过 H2、Mock 和 Stub 隔离验证，MySQL 8 结构迁移已随 Flyway 完整链成功执行；尚未取得 TDengine 3.2.3 覆盖语义、排序、查询计划及真实外部资源联调证据，多实例广播属于已确认非目标。
- 多能源活动数据读取已通过 Mock、H2 API、OpenAPI 和迁移契约隔离验证；V33 已随隔离 MySQL 8.4 空库 V01–V38 完整迁移链执行，TDengine 3.2.3 已验证模拟多测点同时间排序、seek 分页、起始锚点及查询计划。V33 只新增独立质量场景且不替任何测点猜测 Q1/Q2；TDengine 原始表当前按“测点 + 事件时间”保存当前事实，不提供重复次数或冲突修正历史，接口因此显式返回 `COALESCED_BY_IDENTITY/NOT_RETAINED`。真实能源测点、现场数据、生产数据规模和长期运行仍未验证。
- 能源字典与测点品种绑定已通过 H2 服务、API、OpenAPI、权限/职责、失败路径和迁移契约隔离验证；V34 已随隔离 MySQL 8.4 空库 V01–V38 完整迁移链执行并确认关键闭环表可用。首批品种粗分类映射、单位适用性、`Nm3` 标准状态和全部兼容矩阵仍待能源专家确认；现有模拟审核与模拟绑定只证明软件拒绝和版本链路，不代表生产规则、真实测点或现场验收。
- 折标参数与确定性 `tce` 核心已通过 H2 服务、API、OpenAPI、权限/职责、固定比例单位换算、失败路径和 V35 迁移契约隔离验证；V35 已随隔离 MySQL 8.4 空库 V01–V38 完整迁移链执行。29.3076 GJ/tce、固定源低位热值、电力 3.6 MJ/kWh 和热力 1000 MJ/GJ 均只是带来源的待专业确认研发基线；现有模拟审核和计算只证明确定性算法、版本证据与安全拒绝，不代表正式折标结果、专业验收、真实建筑或现场验收。
- 计量边界汇总和多维查询已通过总分表去重、未分配、残差、购入/外送/净值、历史关系版本、H2 持久化、API/OpenAPI 和 V38 迁移契约隔离验证；V38 已随隔离 MySQL 8.4 空库 V01–V38 完整迁移链执行，并验证关键索引、状态约束和查询计划。现有边界、汇总口径和输入均为研发模拟；真实关系图、真实封账结果、专业总分表口径、生产容量和现场验收仍未验证，不能据此宣布正式生产能源管理闭环完成。
- 碳管理本次目标引擎验收 24 项中 9 项通过、15 项断言失败，不能用普通自动化回归通过替代软件验收。正常容量 12 组合、192 次 HTTP 请求均成功，单请求 500 快照/客户端 16 并发组合最大响应约 2.85 秒；数据库等待连接线程峰值 5，证明实际存在短时排队。100 条变化冻结前归并为 24 项/3 批并完成审批发布，但重启恢复、冻结后变化、失败隔离、超时和并发幂等仍有阻断，须修复后按同一设计复验。此为合成快照、隔离 MySQL 和测试配置证据，不证明生产 SLA、长期调度或真实建筑核算；未取得正式因子、分母及真实核算样例前，不得发布正式碳结果。
- 标准设备参数治理已形成后端活动候选并通过 H2、Mock 和 Stub 隔离验证，MySQL 8 结构迁移已随 Flyway 完整链成功执行；现有设备表的额定容量、额定功率和设计 COP 只作兼容投影，旧写入口已拒绝直接修改。TDengine 3.2.3 执行语义、真实厂家报文/Excel、历史生产数据迁移、专业参数及公式依赖、真实外部资源联调和独立验收仍未验证，不能据此宣布生产参数闭环完成。
- 后台职责与审计治理公共基础候选已覆盖职责、账号开通/停用/恢复、密码重置令牌、正式角色、建筑范围、菜单定义和角色菜单敏感申请；设备产品启停、待认领设备正式绑定和设备身份启停也已复用同一申请闭环，并对上述旧直改入口稳定拒绝。账号开通申请包经一次审批后原子创建账号、角色和建筑授权，凭据只通过一次性令牌交付；设备接入继续由原领域事务校验产品、设备、身份、测点和可信建筑归属，建筑访问审核继续沿用原领域状态机，均不重复创建领域状态机。采集治理和质量使用策略均已关闭直接发布，复用公共职责但保留各自领域审核事实；V27、V28 已在隔离 MySQL 8 空库随全部迁移链执行并验证新检查约束。系统安全、采集、质量使用、设备参数、设备接入和关系治理六类权威来源已接入联邦查询与默认脱敏异步导出，并通过职责动态撤销、建筑越权拒绝、游标分页和脱敏下载的 H2/API 自动化验证；V29 已验证公共字段和导出任务表，V30 已实现不可改写保留策略版本、保全范围、清理批次证据和受控分批删除，并在一次性隔离 MySQL 8.4 空库随完整迁移链执行；每日 10 万条合成容量与第 100001 条关键事件、查询索引、存储分配、保全排除和分批清理已经隔离验证，生产数据库迁移、生产容量承诺和独立候选验收仍未完成。
- 能源规则和数据边界未确认会直接阻塞评价、诊断、能流和碳排结果验收。
- 空间与语义关系治理已形成后端活动候选并通过 H2、API 契约、平台 V1 Excel 模板渲染和隔离 MySQL 8 验证；V24 关系治理与 V32 表计结构从空库随完整 Flyway 链执行，并验证治理表、关键外键、字符排序规则、表计层级/方向、导入原子性、失败回滚和正常原子生效。当前有效计量分配接口已用模拟建筑、测点、边界、分配和未分配事实完成 H2 软件验证；该查询本身尚未在 MySQL 8、真实建筑或现场硬件环境执行。独立候选验收、数值化规模测试、能源专家真实表格适配、关系类型/证据确认和真实计量图映射仍未完成；前端维护页面、计量分摊、动态碳因子和园区级跨建筑关系仍无实现，不能据此宣布真实业务、碳核算或现场闭环完成。
- GitHub Ruleset `Protect main` 和 `Protect baseline tags` 已启用。`main` 必须通过 PR 合并、解决全部审查对话、基于最新主线并通过 `Java 21 verify`、`Frontend verify`、`Repository guardrails`；基线标签允许创建但禁止更新和删除。Ruleset 配置不能代替每个 PR 的实际检查结果。
- 自动化测试不能证明现场网络、真实设备、硬件时钟、TLS 和长期运行稳定性。
- MQTT TLS/V2 候选已完成隔离 EMQX、临时证书、TLS 主机名校验、Paho QoS 1 往返，以及隔离 MySQL 8/TDengine 3.2.3 联合运行；模拟双测点报文取得 `PLATFORM_PERSISTED`、MySQL 终态回执和 TDengine 两条原始行。V23 已在隔离 MySQL 8 从 V22 升级并验证重复迁移无变化、24 小时热回执持续清理、异常关联保护和 180 天异常删除；软件热路径模拟达到本期短压测目标，但不是生产容量承诺。当前证据仍不包含生产保留期批准、生产 Broker/证书、真实设备 ACK 能力、真实网络抖动、长期稳定性或网关断电补传。
- MySQL Flyway 已验证空库完整迁移、非空旧库默认拒绝、V3 baseline 后继续迁移，以及已人工存在 V22 结构时的幂等接管；真实生产库仍必须单独备份、结构核验和演练，不能直接开启 baseline。

## 8. 下一步

1. 在隔离目标环境继续验证数据源、采集策略和能源采集元数据的读写及运行快照隔离语义，并完成独立验收后再决定是否批准为当前正式能力；V31 随空库 V01-V32 完整 MySQL 8 迁移链执行的验证已完成。
2. 在隔离目标环境验证 Q0/Q1/Q2 使用策略治理的 MySQL 8 与 TDengine 3.2.3 执行语义和查询计划，并完成独立验收后再决定是否批准为当前正式能力。
3. 在取得经专业确认的真实能源测点和现场数据后，复验活动数据质量门禁、计量事件、迟到修正和长期运行；当前隔离 MySQL 8.4 与 TDengine 3.2.3 结果只作为研发软件收口证据，不描述为现场验收。
4. 在生产候选 Broker、正式账号和 Topic ACL 环境复验 V2 应用 ACK，并另行开展真实设备、真实网络和长期稳定性验收，再决定是否进入独立候选验收。
5. 在隔离目标环境验证标准设备参数治理的 MySQL 8 与 TDengine 3.2.3 双时间查询、受控追溯和重算语义，并完成独立验收后再决定是否批准为当前正式能力。
6. 继续锁定首批标准参数语义、设备类型适用范围、单位、精度与边界、公式参数依赖、厂家报文、Excel 样例和验收数据；在专业确认前保持配置框架、遗留数据无损暂存和拒绝路径，不宣布生产参数闭环完成。
7. 对后台职责与统一审计治理固定合并基线开展独立候选验收；生产迁移前另行确认账号分工、适用制度、备份和容量参数，并验证恢复旧备份后的重新清理流程。
8. 取得能源专家真实表格、专业关系证据和真实计量图后，另行评估模板适配并完成现场映射；同时补充建筑级关系治理的数值化规模边界和独立候选验收，继续拒绝未确认分摊规则及园区级跨建筑关系。
9. 阶段一形成可信数据后再进入可视化，不用静态页面或假数据提前宣称产品完成。
10. 由能源专家确认首批品种映射、单位适用性、兼容矩阵、标准煤低位热值和折标参数，并补充真实输入与预期 `tce` 验收样例；确认前保持待确认、生产不可用和安全拒绝。
11. 先修复[碳管理软件验收报告](docs/reviews/carbon/2026-09-03-software-acceptance.md)已复现的阻断，再按冻结设计重跑容量、积压归并、实际重启与独立软件验收；当前独立结论为不通过。正式因子、完整因子组合、真实建筑分母/封账快照及预期 CO2e 样例另行取得专业确认后复验，生产等价环境长期容量与现场验收仍需补充。可视化继续留在后续阶段。

## 9. 更新规则

- 只将有当前代码、测试或有效验收证据的事项标为已实现。
- 阶段、完成项、风险或下一步变化时更新本文件；稳定架构和链路变化更新 `PROJECT_GUIDE.md`。
- 不记录固定测试数量、本机端口、容器状态、密码、Token、个人路径或临时排查过程。
- 旧仓库后续变化不会自动改变本项目状态；需要复用时必须在独立任务中评估和迁移。
