# iot-platform-demo 项目全局规则

本文件中的规则适用于整个仓库。除非用户明确给出更高优先级的指令，否则所有任务都必须遵守这些规则。

## 规则一：Git 开发与提交流程

### 1. 核心原则

所有开发必须遵循：

AI 同步 `main` → 创建短期任务分支 → 编码、测试、提交 → 推送任务分支 → AI 提供 PR 链接、标题、说明和测试结果 → 用户创建并合并 PR → 用户通知 AI“已合并” → AI 同步本地 `main` 并删除本地任务分支

`main` 只保存已完成、已测试、可以演示或交付的代码。

禁止直接在 `main` 上开发、提交或推送。

默认分工：

- AI 负责同步本地 `main`、创建任务分支、编码、测试、提交和推送任务分支。
- AI 负责准备并发送 PR 创建链接、建议标题、PR 说明和测试结果。
- 用户负责创建 PR、完成审核并合并 PR。
- 用户合并后明确告诉 AI“已合并”。
- AI 只有在收到“已合并”通知后，才同步本地 `main` 并删除本地任务分支。
- 除非用户明确改变分工，否则 AI 不得代替用户创建或合并 PR。

### 2. 开始任务

开始编码前必须：

1. 检查工作区状态。
2. 确认没有混入其他任务的改动。
3. 获取最新远程状态。
4. 切换到本地 `main`，确认它可以安全快进到最新 `origin/main`。
5. 使用仅快进方式同步本地 `main`。
6. 从同步后的 `main` 创建短期任务分支。
7. 创建分支后才允许修改代码。

示例：

```bash
git fetch --prune origin
git switch main
git merge --ff-only origin/main
git switch -c feature/hvac-export
```

首次使用仓库或本地 Hook 未安装时，在当前仓库执行：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/Install-GitGuardrails.ps1
```

创建任务分支且工作区仍然干净时，执行统一预检：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/Invoke-TaskPreflight.ps1
```

预检失败时必须先修复报告的问题。预检成功只证明分支、基线、工作区、规则入口和 Hook 状态符合机械条件，不能替代 AI 实际阅读并理解规则。

如果本地 `main` 与远程分叉、有未推送提交或工作区存在不明改动：

- 不得直接覆盖、重置、变基或合并；
- 不得继续创建包含无关提交的任务分支；
- 必须先报告状态并隔离已有工作；
- 如主工作目录正在被其他任务使用，可以在报告后从最新 `origin/main` 创建隔离 worktree 和任务分支。

### 3. 分支命名

按任务性质命名：

- `feature/<name>`：新增功能
- `fix/<name>`：缺陷修复
- `perf/<name>`：性能优化
- `refactor/<name>`：代码重构
- `docs/<name>`：文档或代码注释
- `test/<name>`：测试建设
- `chore/<name>`：构建、配置和维护工作

示例：

- `feature/hvac-history-export`
- `fix/hvac-time-range`
- `perf/hvac-query-cache`
- `docs/hvac-query-code-comments`

分支表示“一次施工任务”，不代表永久模块。禁止长期保留模块分支。

### 4. 任务边界

一个分支只能处理一个明确任务。

禁止：

- 把多个无关功能放入同一个分支；
- 顺手提交无关格式化、重构或配置修改；
- 把其他任务的未提交文件一起暂存；
- 使用 `git add .` 随意暂存全部文件。

应明确列出本次任务文件：

```bash
git add -- <file1> <file2> <file3>
git diff --cached --name-only
git diff --cached --check
```

### 5. 设计与计划

以下任务需要先确认设计和范围：

- 新模块；
- 新 API；
- 数据库结构变化；
- 权限、缓存、并发或数据流程变化；
- 跨多个子系统的复杂功能。

以下任务可以跳过设计说明和实施计划：

- 纯代码注释；
- 纯文档修正；
- 拼写修复；
- 不改变行为的简单格式调整。

跳过设计不代表可以跳过分支、测试、提交和 PR 流程。

### 6. 提交规范

提交信息采用：

`type(scope): 简短说明`

常用类型：

- `feat`：功能
- `fix`：修复
- `perf`：性能
- `refactor`：重构
- `docs`：文档或注释
- `test`：测试
- `chore`：维护

示例：

- `feat(hvac): add history export API`
- `fix(hvac): validate history time range`
- `docs(hvac): explain query API flow`
- `test(config): verify datasource isolation`

每个提交必须：

- 内容单一；
- 信息明确；
- 能独立解释其目的；
- 不包含生成文件、临时文件或无关文件；
- 提交前完成对应测试。

仓库 Hook 会拒绝在 `main` 提交，并对暂存差异执行机械检查；pre-push 会拒绝任何目标为远程 `main` 的更新。不得使用 `--no-verify` 绕过失败；如果 Hook 报错，应根据具体规则修复代码、差异或本地安装状态。

### 7. 测试要求

提交前至少执行与改动直接相关的定向测试。

涉及生产代码时，还应执行完整回归测试：

```bash
mvn test
```

提交或 PR 中必须如实记录：

- 执行了什么测试；
- 通过数量；
- 是否存在跳过项；
- 未执行测试的原因。

测试失败时不得推送为可合并 PR，不得隐瞒失败结果。

### 8. 推送和 PR 材料交付

任务完成并通过测试后，必须推送远程任务分支：

```bash
git push -u origin <branch-name>
```

不得只保留本地提交。

推送成功后，AI 不直接创建 PR，而是向用户发送完整的 PR 材料：

- 可直接打开的 GitHub Compare/PR 创建链接；
- Base：`main`；
- Compare：当前任务分支；
- 建议的 PR 标题；
- 可直接复制的 PR 说明；
- 变更内容；
- 明确说明不包含的范围；
- 测试命令、通过数量、跳过项和未执行项；
- 当前是否存在合并冲突；
- 无关文件检查结果。

生产 Java、Vue 或 TypeScript 文件发生变化时，必须为本任务新增一份长期保存的中文注释审计文档。固定路径格式为：

```text
docs/reviews/comment-audits/<year>/<YYYY-MM-DD>-<task>.md
```

使用以下命令生成模板：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/New-CommentAuditReport.ps1 -BaseRef origin/main -HeadRef HEAD -OutputPath docs/reviews/comment-audits/2026/2026-08-06-example.md
```

报告必须覆盖每个变化生产文件和扫描器识别出的全部方法、构造器及函数。不能保留“待填写”或“待核验”；关键项填写注释处理结果和业务说明，简单项填写具体免注释原因。文档必须在当前 PR 中相对 base 新增，不能复用已经存在于 base 的历史报告；同一 PR 后续提交改变生产代码时，必须同步更新这份文档。

PR 的“注释审计”章节只放一个指向该文档的仓库相对 Markdown 链接。没有生产代码变化时填写“不涉及生产代码”，不创建空审计文档。CI 通过只代表结构和证据完整，不能代替主代理判断注释是否真实、准确并与当前实现一致。

PR 创建链接格式：

```text
https://github.com/<owner>/<repo>/compare/main...<branch-name>?expand=1
```

AI 发送上述材料后，应明确说明当前状态为“等待用户创建并合并 PR”，不得声称整个任务已经完成。

默认不能绕过 PR 直接更新远程 `main`。

### 9. 合并规则

用户负责创建和合并 PR。AI 不得代替用户点击创建或合并，除非用户明确改变本次任务的分工。

满足以下条件后才能合并：

- 测试通过；
- PR 内容与任务范围一致；
- 没有冲突；
- 所有对话已解决；
- 没有敏感信息或临时文件；
- 没有无关提交。

默认使用 GitHub 的 `Merge pull request`，保留完整 PR 边界。

如果任务分支包含大量临时提交，应先整理提交，避免把无意义历史带入 `main`。

如果 PR 审核产生修改要求，AI 应继续在原任务分支完成修改、测试、提交和推送，并向用户更新 PR 说明与测试结果。

### 10. 合并后的清理

只有在用户明确告诉 AI“已合并”后，AI 才执行以下操作：

1. 获取最新远程状态并清理已失效的远程引用。
2. 切回本地 `main`。
3. 确认本地 `main` 可以安全快进。
4. 使用仅快进方式同步本地 `main`。
5. 确认任务分支已经合并且没有未提交改动。
6. 安全删除本地任务分支。
7. 如果任务使用独立 worktree，先确认其中没有未提交改动，再清理该 worktree。

示例：

```bash
git fetch --prune origin
git switch main
git merge --ff-only origin/main
git branch -d <branch-name>
```

如果本地 `main` 无法快进：

- 立即停止；
- 不得自动使用 `reset --hard`、强制变基或强制覆盖；
- 先报告本地和远程的分叉提交。

远程任务分支是否删除由用户的 GitHub 合并设置或用户操作决定。AI 默认只清理本地任务分支，不擅自删除远程分支。

使用仓库清理脚本时，必须先运行默认预览，再在全部检查通过后显式执行：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/Invoke-PostMergeCleanup.ps1 -TaskBranch chore/example -WorktreePath C:\absolute\registered\worktree
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/Invoke-PostMergeCleanup.ps1 -TaskBranch chore/example -WorktreePath C:\absolute\registered\worktree -Apply
```

脚本路径参数必须是 Git 已登记任务 worktree 的精确绝对路径；不得用通配符或宽泛目录代替。

### 11. 禁止操作

除非用户明确授权并已确认恢复方案，否则禁止：

- `git push --force`
- `git push --force-with-lease`
- `git reset --hard`
- 强制移动 `main`
- 删除未合并分支
- 覆盖用户未提交的改动
- 擅自丢弃提交
- 直接推送远程 `main`
- 把密钥、密码、Token 或本地配置提交到仓库

### 12. 完成标准

AI 完成开发并交付 PR 材料时，必须满足：

- 代码或文档已经修改；
- 测试已经通过；
- 已产生明确提交；
- 已推送远程任务分支；
- 已向用户发送 PR 创建链接、建议标题、PR 说明和测试结果；
- 已明确报告当前处于“等待用户创建并合并 PR”状态。

整个任务只有满足以下条件才算完成：

- 用户已经创建并合并 PR；
- 用户已经明确告诉 AI“已合并”；
- AI 已同步本地 `main`；
- AI 已安全删除本地任务分支；
- AI 已向用户报告最终同步和清理结果。

除非用户明确要求“仅本地修改”，否则不得只完成本地提交就停止。AI 推送分支并交付 PR 材料后必须等待用户创建和合并 PR，不得越权完成用户负责的步骤。

## 规则二：现阶段模块化准备与架构边界

### 1. 当前阶段目标

项目当前首要目标是完成一个完整、稳定、可以正常运行和演示的 V1 版本，包括：

- 后端主要功能完成；
- MySQL、TDengine、MQTT 等核心链路跑通；
- 前端功能完成并能正常调用后端；
- 主要业务流程具备自动化测试；
- 开发、测试和部署流程基本稳定。

在 V1 完成前，优先保证功能正确、链路完整和系统可运行。

不得为了追求架构形式，影响当前功能交付进度。

### 2. 当前架构策略

V1 阶段继续保持单体架构。

当前只做低成本的模块化准备，不进行大规模模块拆分，不拆微服务，不集中重写现有代码。

模块化准备的目标是：

- 新代码有明确归属；
- 模块职责尽量清楚；
- 数据源和外部资源边界明确；
- 避免新增循环依赖；
- 避免代码继续无序堆积；
- 为以后正式重构降低成本。

模块化准备不等于立即进行正式模块化重构。

### 3. 新功能归属规则

新增功能前，必须先确定它属于哪个业务模块。

代码应放入对应业务模块，例如：

- 用户、角色、权限放入权限模块；
- 设备、测点、身份映射放入设备或物联网模块；
- HVAC 查询、聚合和指标计算放入 HVAC 模块；
- MySQL、TDengine、MQTT 等接入代码放入对应基础设施模块。

禁止为了方便，把业务代码随意放入：

- `common`
- `utils`
- `config`
- 其他无关业务模块
- 无明确职责的公共目录

如果暂时无法判断代码归属，必须先报告并说明可选位置，不能随意放置。

### 4. 分层职责规则

原则上按照以下职责组织代码：

- Controller：接收请求、校验基础参数、调用 Service、返回结果。
- Service：负责业务规则、流程编排和事务边界。
- Repository、Mapper：负责数据库和时序数据库访问。
- Adapter、Client：负责 MQTT、第三方接口和外部系统交互。
- DTO：负责接口输入和输出。
- Entity、DO：负责数据库持久化结构。
- Config：只负责配置和组件装配，不承载业务逻辑。

禁止：

- Controller 直接访问 Mapper 或 Repository；
- Controller 编写复杂业务计算；
- Repository 编写业务流程；
- Config 类承担业务功能；
- 把所有逻辑集中在一个大型 Service 中。

修改已有代码时不要求一次性全部调整，但新代码不得继续扩大这些问题。

### 5. 模块调用规则

模块之间应优先通过明确的 Service、接口或 DTO 进行调用。

原则上禁止：

- 直接调用其他模块的 Mapper 或 Repository；
- 直接依赖其他模块的内部实现类；
- 随意操作其他模块负责的数据表；
- 模块之间形成循环依赖；
- 为了省事复制另一模块的业务逻辑。

如果现有架构暂时无法避免跨模块直接调用：

1. 必须先说明原因；
2. 优先采用最小影响方案；
3. 不得继续扩大依赖范围；
4. 必要时记录为后续模块化重构事项。

### 6. 公共代码规则

公共能力只有在出现真实复用需求后才能提取。

满足以下条件时才考虑提取公共代码：

- 已经有两个或以上模块真实使用；
- 业务含义稳定；
- 不属于某个具体业务模块；
- 提取后不会产生反向依赖或循环依赖。

禁止：

- 因为“以后可能会使用”而提前抽象；
- 创建职责模糊的万能工具类；
- 把业务逻辑包装成公共工具方法；
- 把不同业务仅仅因为代码相似就强行合并。

无法确定是否应该公共化时，优先暂时保留在业务模块内部。

### 7. 数据源边界规则

MySQL 和 TDengine 必须保持明确边界。

- MySQL 负责业务配置、用户权限、设备档案、测点档案等结构化业务数据。
- TDengine 负责原始时序数据、分钟数据和指标数据。
- 使用多个 `DataSource` 或 `JdbcTemplate` 时，必须明确指定 Bean 名称和 `Qualifier`。
- 禁止仅依靠类型自动注入来判断使用哪个数据源。
- MySQL SQL 不得发送给 TDengine。
- TDengine SQL 不得通过 MySQL 数据源执行。
- 数据访问代码应通过对应 Repository、Mapper 或专用数据访问组件完成。

新增数据访问代码时，必须能够直接判断它使用的是哪个数据库。

### 8. 外部资源和启动行为

MySQL、TDengine、MQTT、Redis 和第三方接口都属于外部资源。

外部资源相关功能必须遵守：

- 地址、端口、账号和功能开关通过配置管理；
- 禁止在业务代码中硬编码环境地址和密码；
- 启动期建库、建表、数据修复和外部连接应提供配置开关；
- 某个外部资源不可用时，不应无条件拖垮无关模块；
- 是否允许降级必须根据业务场景明确决定，不能静默吞掉关键错误；
- 重要启动行为需要添加直白的中文注释。

### 9. 自动化测试隔离

普通自动化测试不得依赖真实外部环境。

测试环境原则上不得主动连接：

- 真实 MySQL；
- 真实 TDengine；
- 真实 MQTT Broker；
- 真实 Redis；
- 真实第三方接口。

测试应优先使用：

- H2；
- Mock；
- Fake 实现；
- 测试专用配置；
- 可控的集成测试容器。

只有明确标记的集成测试，才允许连接专用测试环境。

新增外部资源功能时，必须同时考虑测试环境如何关闭、替换或模拟该资源。

### 10. 配置开关规则

以下功能应优先提供明确配置开关：

- 启动时自动建库、建表；
- 数据库结构检查或修复；
- 后台定时任务；
- 数据恢复和补偿任务；
- MQTT 自动连接；
- 模拟器和演示数据；
- 依赖外部服务的初始化任务。

生产环境可以保持默认开启，但测试环境必须可以关闭。

配置项旁应使用中文说明：

- 开启后执行什么；
- 关闭后停止什么；
- 生产和测试环境有什么区别。

### 11. 注释和可理解性

以下代码必须添加直白的中文注释：

- 多数据源选择；
- 特殊 Spring 注入方式；
- 配置开关；
- 启动初始化流程；
- 重试、降级和补偿逻辑；
- 不容易理解的业务规则；
- 为保持模块边界而采用的特殊写法。

注释重点解释“为什么这样做”和“不这样做会出现什么问题”。

禁止只把代码翻译成中文，而不说明设计原因。

### 12. 控制改动范围

一个任务只解决一个明确问题。

开发过程中发现其他架构问题时：

- 可以记录；
- 可以说明风险；
- 可以提出后续建议；
- 不得未经确认顺手进行大范围重构；
- 不得把无关模块一起修改；
- 不得借新增功能之机重写整个模块。

如果小范围整理能够直接降低当前功能风险，可以提出方案，经用户确认后实施。

### 13. 技术债处理

发现以下问题时，应记录为技术债或后续重构事项：

- 模块循环依赖；
- Service 体积过大；
- 跨模块直接访问 Repository；
- 公共目录职责混乱；
- DTO、Entity 混用；
- 外部资源无法替换或测试；
- 重复业务逻辑；
- 配置和业务逻辑耦合；
- 单个类承担过多职责。

记录技术债不代表当前必须马上重构。

当前任务优先完成业务目标，同时避免继续扩大问题。

### 14. 正式模块化重构条件

只有满足以下基本条件后，才集中考虑正式模块化重构：

- V1 后端功能基本完成；
- 主要业务链路全部跑通；
- 前端已经完成并能正常使用；
- 系统可以稳定启动和演示；
- 主要模块具备回归测试；
- 数据库和外部接口边界已经基本明确；
- 当前架构问题已经有清单和优先级。

正式重构前必须单独设计、评估影响并制定迁移计划。

不得一边开发大量新功能，一边进行无边界的大规模架构重写。

### 15. 微服务判断规则

微服务不是项目后续必然要做的事情。

只有出现以下明确需求时才考虑微服务：

- 某个模块必须独立部署；
- 某个模块需要独立扩容；
- 某个模块故障必须与主系统隔离；
- 不同团队需要独立开发和发布；
- 模块数据边界已经稳定；
- 单体已经出现明确且无法通过普通优化解决的瓶颈。

不能仅因为“项目以后会变大”就提前拆微服务。

拆分微服务前必须评估：

- 部署复杂度；
- 服务通信；
- 数据一致性；
- 分布式事务；
- 日志和链路追踪；
- 故障处理；
- 运维成本；
- 测试成本。

如果收益不能明显覆盖复杂度，不进行微服务拆分。

### 16. 边界冲突处理

如果新需求会突破现有模块边界、引入循环依赖或导致大范围修改，AI 必须先停止实施并向用户说明：

- 当前需求涉及哪些模块；
- 为什么现有边界无法直接承载；
- 最小改动方案；
- 小范围整理方案；
- 正式重构方案；
- 各方案的成本和风险。

未经用户确认，不得自行选择大规模重构方案。

### 17. 当前阶段完成标准

当前阶段的正确做法是：

- 第一版优先跑通；
- 新代码保持清晰归属；
- 数据源和外部资源边界明确；
- 自动化测试与真实环境隔离；
- 不新增明显的循环依赖；
- 不继续扩大已有混乱；
- 对未来重构保留必要接口、配置和测试基础；
- V1 完成后再集中评估正式模块化重构。

核心原则：

**现在保持边界、控制混乱、完成 V1；第一版完整可运行后，再集中进行正式模块化重构。**

## 规则三：用途优先的业务代码注释

### 1. 注释目标

新增或修改代码时，必须从业务阅读者的角度检查相关注释是否足以帮助理解代码用途。

注释的主要目标是帮助用户弄清楚：

- 当前类或方法负责什么；
- 为什么需要这一层或这段逻辑；
- 它在完整业务流程中的位置；
- 它与上下游模块、数据源或外部资源的关系；
- 数据由谁传入、经过哪些关键接口或实现类、最终交给谁；
- 有哪些无法直接从代码中看出的业务限制、异常含义或设计原因。

AI 生成或修改代码时，必须把实现过程中掌握的设计上下文同步写入代码注释，不能只交付
能够运行的实现，再让后续维护者通过全文搜索和逐行阅读重新推导调用链、数据来源和设计原因。

注释数量不是目标。应优先保证信息有效、准确和容易理解，避免为了满足形式要求而增加冗余注释。

### 2. 类级注释

新增业务类时，应添加简洁、直白的中文类级注释。

修改已有业务类时，应检查现有类级注释是否仍能准确说明用途；如果职责或业务流程发生变化，必须同步更新注释。

类级注释应根据实际情况说明：

- 该类负责的业务能力；
- 谁调用该类，以及结果会交给哪个页面、接口、任务或下游模块；
- 它处于 `Controller → Service → Repository → 数据库` 调用链中的哪个位置；
- 它与其他业务模块、数据源或外部系统的关系；
- 它不负责哪些容易混淆的职责。

如果一个类包含较多业务方法，类级注释还应概括主要流程阶段或方法分组，让阅读者先看注释
就能找到主线，不能要求阅读者在几十个方法中自行判断哪些方法构成完整流程。

禁止只写“某某 Controller”“某某 Service 实现类”这类不能帮助理解业务用途的注释。

### 3. 不同代码层重点说明的内容

根据代码所在层次，注释应优先说明以下内容：

- Controller：接口用途、主要参数的业务含义、权限入口以及调用的核心业务能力。
- Service：业务校验、流程编排、事务边界、查询范围校验、结果组装、关键业务决策，以及各主要阶段调用的下游接口。
- Repository、Mapper：访问 MySQL 还是 TDengine、查询对象、时间区间语义、聚合方式以及特殊数据限制。
- Adapter、Client：交互的外部系统、调用目的、失败影响、重试或降级策略。
- DTO：字段的业务含义、单位、取值范围、是否允许为空以及字段之间的约束。
- Entity、DO：对应的持久化对象、关键字段含义以及容易误解的数据约定。
- Config：配置控制的功能、开启和关闭的效果，以及生产环境和测试环境的差异。
- Page、Component：页面或组件的业务目标、数据由哪个 Composable、Store 或 API 提供、关键展示字段对应的业务编码，以及加载、无数据、部分失败和过期状态的展示方式。
- Composable、Store：状态生命周期、初始化和销毁入口、调用的 API、请求之间的编排关系、切换对象时的清理规则，以及浏览器持久化副作用。
- 前端 API、Request：对应的后端路径、响应数据用途、Token 注入、统一错误处理、401 清理和跳转副作用。
- Router：公开路由、认证恢复、角色限制和重定向规则，以及这些规则与后端权限校验的边界。
- 前端 DTO、Type：字段来自哪个后端响应、业务含义和单位；不得把页面展示数量误写成后端接口的固定返回范围。

### 4. 关键方法判定和方法级注释

关键方法是否必须添加说明，只由它承担的职责决定，不由 `public`、`protected`、`private`
可见性决定。私有方法只要承担完整业务步骤，也必须按照关键方法处理。

满足以下任一条件的方法属于关键方法：

- 业务流程入口或主要处理阶段；
- 权限、角色、建筑范围、数据归属或参数组合校验；
- 状态变化、事务边界、幂等、并发控制、重试、补偿或恢复；
- MySQL、TDengine、Redis、MQTT、HTTP、文件等数据源或外部资源访问；
- DTO、Entity、时序行、缓存值、外部报文或前端展示模型之间的重要转换；
- 聚合、公式、分辨率选择、时间区间或数据质量处理；
- 异常转换、降级、兜底、空数据和部分成功处理；
- 方法名无法完整表达的业务限制、副作用或设计选择；
- 虽然只在类内使用，但被一个或多个主流程依赖，并承担可以单独描述的业务步骤。

关键方法注释应根据实际情况说明：

- 该方法完成完整流程中的哪个业务步骤；
- 输入从哪个上游传入，关键参数采用什么业务语义；
- 调用了哪个下游接口、实现边界、数据源或外部系统；
- 为什么采用当前方案，不这样处理会产生什么权限、数据或一致性风险；
- 返回值交给谁，空值、状态码和异常分别代表什么；
- 是否存在事务、缓存、跨库一致性、重试、异步或其他副作用。

方法体中的行内注释只能补充局部分支，不能代替关键方法整体契约。简单 getter、setter、
单行委托和已经由命名完整表达含义的方法，不属于关键方法，不得为了覆盖率机械添加说明。

### 5. 必须补充或更新注释的场景

新增或修改以下代码时，必须添加或同步更新中文注释：

- 新增业务类、业务接口或关键方法，包括承担关键职责的私有和受保护方法；
- 关键业务流程及其主要步骤；
- 复杂条件分支、状态变化和业务决策；
- 不容易直接看懂的数据转换、聚合或结果组装；
- 权限判断、数据范围限制和参数组合约束；
- 多数据源选择和跨模块调用；
- 外部资源调用、重试、降级、补偿和异常处理；
- 特殊算法、性能优化或为保持模块边界采用的特殊写法；
- 返回 `400`、`403`、`404`、`503` 等状态的业务原因；
- 代码行为已经变化，但原有注释可能不再准确的地方。

已有注释准确且已经说明相同内容时，不得重复添加。

### 6. 业务限制和异常说明

无法从方法名或代码结构直接看出的业务限制，必须在最接近对应逻辑的位置说明。

例如：

- 最多允许选择多少个测点；
- 最长允许查询多长时间；
- 时间范围采用 `[from, to)` 还是其他区间语义；
- 使用哪种分辨率、聚合方式或默认值；
- 为什么某种输入返回 `400`、`403`、`404` 或 `503`；
- 为什么必须访问 MySQL、TDengine 或特定外部资源；
- 某个失败是否允许降级，以及降级后会产生什么影响。

注释应说明规则背后的业务原因或风险，不能只重复条件表达式。

### 7. 前后端调用链和数据传递说明

重要功能必须能够从用户入口或外部事件追踪到最终数据源或外部系统，再追踪结果返回路径。
注释应分布在各层职责附近，不要求在一个文件复制整条链路，但入口类和关键方法必须提供
足够的上下游信息，使维护者可以继续定位，而不是依靠猜测或全仓库搜索。

前端关键展示值应能够沿以下路径追踪：

`模板展示 → 页面辅助方法或计算属性 → Composable 或 Store → API → 后端 Controller → Service → Repository/Mapper → 数据源字段`

后端写入或异步任务应能够沿以下路径追踪：

`请求或外部事件 → Controller/Adapter/Listener → Service/Executor → Repository/Client → 数据源或外部系统 → 状态、事件或响应`

当一个功能跨越三个以上关键层，或包含异步、跨库、状态机等复杂流程时，应同时保留设计文档
或模块流程说明。文档负责全局视图，代码注释负责当前实现和当前位置，两者不能互相替代。

### 8. 不需要添加注释的内容

以下内容通常不需要添加注释：

- 普通 getter、setter；
- 一眼可以看懂的赋值、判空或简单调用；
- 框架注解和框架固定模板的基础含义；
- 已经由清晰命名完整表达用途的简单代码；
- 与附近注释重复的内容；
- 把 Java、SQL 或前端代码逐行翻译成中文；
- 具体实现步骤，除非算法、业务规则或设计原因确实难以理解。

不得为了让每一段或每一行都有注释而制造重复、过时或干扰阅读的说明。

### 9. 注释写法

注释统一使用简洁、直白的中文，优先回答：

- 这里是做什么的；
- 为什么需要这样做；
- 它在完整业务流程中起什么作用；
- 不这样做会出现什么业务问题；
- 调用方需要知道哪些限制或副作用。

注释应靠近它所解释的类、方法、字段或逻辑。

禁止：

- 使用空泛表述；
- 只复述类名、方法名或代码语句；
- 保留与当前实现不一致的旧注释；
- 使用“当前任务”“本次修改”等开发过程历史代替类和方法的长期职责；
- 把尚未接入业务的预留能力描述成已经生效的功能；
- 使用注释掩盖过度复杂、命名混乱或职责不清的代码；
- 大段复制需求文档，却不说明当前代码承担的具体职责。

### 10. 修改代码时的注释检查

每次新增或修改代码后，AI 必须检查：

1. 新增或改变的业务职责是否已经说明；
2. 是否列出了本次涉及类的全部方法，并按职责而不是可见性判断哪些属于关键方法；
3. 关键方法在整体调用链中的位置、输入来源、下游调用和输出去向是否清楚；
4. 关键展示或写入字段从产生、转换、聚合、缓存到展示或持久化的路径是否清楚；
5. 隐含业务规则、数据边界、状态变化、异常原因和副作用是否已经说明；
6. 原有注释是否仍与代码行为、接口范围、配置效果和字段用途一致；
7. 是否出现重复注释、逐行翻译、任务历史、未来设想或无实际信息的注释；
8. 大型类是否能够从类级注释和关键方法说明中找到主流程，而不必先阅读全部实现。

如果代码本身已经通过清晰命名和简单结构完整表达用途，可以不增加重复注释，但 AI 应在交付结果中如实说明已完成注释检查。

只要生产文件发生变化，就必须检查该文件的完整当前内容，而不是只检查 diff 新增行。文件内全部可识别方法、构造器和函数都必须进入审查清单，包括私有方法；已有历史注释也必须复核时效，已经实现的目标改写为当前真实行为，已经取消或过期的承诺删除。

提交或 PR 说明中还应列出每个变化生产文件、完整方法清单和逐项判定，并说明哪些简单方法被有意判定为不需要注释及具体原因。不能只用“类上有 Javadoc”“公开方法有注释”或注释行数比例作为合格证据，也不能把扫描器生成的“待填写”报告直接作为审查结果。

### 11. 完成标准

注释达到以下效果才算合格：

- 阅读类级注释能够理解该类的整体职责；
- 阅读关键方法注释能够理解其业务用途、输入来源、下游调用、数据源和输出去向；
- 阅读关键逻辑附近的注释能够理解特殊限制和设计原因；
- 前端关键展示值能够追踪到后端接口、业务服务、数据访问层和最终数据源字段；
- 权限、状态机、跨库、异步、缓存和外部资源边界具有就近说明；
- 关键私有或受保护方法没有因为可见性被遗漏；
- 不需要逐行阅读全部实现，就能大致判断这段代码在完整业务流程中的作用；
- 注释简洁、准确，没有明显重复，也不会使代码过度臃肿。

## 规则四：项目信息分层与维护

### 1. 信息职责

项目相关信息必须按以下职责保存，禁止把所有内容混入同一个载体：

- Codex Memories：只保存用户个人偏好、一般背景和跨项目仍可复用的经验，不保存当前项目的提交号、PR、测试数量、文件进度和本地运行状态。
- `AGENTS.md`：保存长期固定的工作规则，不记录具体任务进度。
- `PROJECT_GUIDE.md`：保存稳定项目地图，包括业务范围、模块边界、核心数据链路、数据源职责、主要入口和文档导航。
- `PROJECT_STATUS.md`：保存其所在 Git 版本可以验证的当前阶段、完成项、未完成项、阻塞、风险、技术债和下一步。
- `docs/superpowers/specs`、`docs/superpowers/plans`：保存具体任务在当时确认的设计、取舍、实施步骤和验证方案，属于任务级历史记录。
- `docs/reviews/comment-audits`：保存生产代码任务合并时的完整中文注释审计快照；历史报告不能复用或回写，行号只对应当时版本。
- 当前会话：保存正在执行的命令、错误、临时判断、排查过程和尚未进入 Git 的具体任务状态。

### 2. 任务开始时的读取顺序

处理本项目任务时，AI 必须按以下顺序建立上下文：

1. 读取 `AGENTS.md`，确认固定工作规则。
2. 读取 `PROJECT_GUIDE.md`，定位业务模块、数据链路、数据源和详细文档。
3. 读取 `PROJECT_STATUS.md`，确认当前阶段、已完成范围、未完成项和风险。
4. 只在当前任务需要时读取对应的专题设计、实施计划和代码，不得无目的加载全部历史文档。

如果入口文件与当前代码发生冲突，不能直接沿用旧描述，必须先核验代码、测试和最近已合并决策。

### 3. 任务执行与交付时的更新规则

任务执行和交付时按照变化性质更新信息：

- 项目定位、模块职责、数据源边界、核心数据流、长期运行入口或文档导航变化时，在同一任务分支更新 `PROJECT_GUIDE.md`。
- 项目阶段、完成项、未完成项、阻塞、风险、技术债或下一步变化时，在同一任务分支更新 `PROJECT_STATUS.md`。
- 新模块、新 API、数据库结构、权限、缓存、并发或数据流程变化仍按规则一先形成任务级设计和实施计划。
- 生产 Java、Vue 或 TypeScript 发生变化时，新增并完成人工填写的任务级注释审计文档；合并后的历史文档不再修改。
- 当前会话中的命令、错误日志和临时排查结论不得机械复制到 Git 文档；只提炼长期有效且有证据支持的结论。
- 只有用户个人偏好或跨项目通用经验发生变化时，才允许按 Codex Memories 的受支持方式提出记忆更新。

文档与代码一起经过分支、测试、提交和 PR 流程。某项结论只有进入当前 Git 版本后，才能作为后续任务依赖的项目事实。

### 4. 冲突和过期处理

项目信息冲突时按以下规则处理：

- 当前代码、自动化测试和已合并决策是核验项目行为的直接证据。
- `PROJECT_GUIDE.md` 负责稳定结构，`PROJECT_STATUS.md` 负责动态状态；两者不得互相复制形成两套当前事实。
- `docs/superpowers` 中的设计和计划记录任务当时的上下文。早期内容与当前实现不一致时，优先视为历史演进记录，不得未经核验批量改写或删除。
- `PROJECT_STATUS.md` 中缺少代码、测试、提交或有效文档证据的事项必须标记为“待核验”，不得推测为已完成。
- Codex Memories 与仓库事实冲突时，以当前 `main` 的代码和项目文档为准；Memories 只用于理解个人偏好和通用背景。

### 5. 安全边界

以下信息不得写入 `PROJECT_GUIDE.md`、`PROJECT_STATUS.md`、任务设计、实施计划或 Codex 记忆纠正说明：

- 密码、Token、私钥、数据库凭据和未脱敏生产数据；
- 仅属于个人电脑的临时端口占用、进程状态和敏感环境信息；
- 临时备份的绝对路径；
- 无法验证来源的完成声明和运行结果。

公开的示例端口和配置项只有在它们属于仓库配置契约时才能记录，并应引用示例配置，不得复制个人本地配置。

### 6. 完成检查

每个任务交付前，AI 还必须检查：

1. 本次变化是否需要更新 `PROJECT_GUIDE.md` 或 `PROJECT_STATUS.md`；
2. 稳定信息和动态状态是否被写入了正确文件；
3. 是否把历史设计、当前事实和未来计划混写在一起；
4. 是否重复记录了可以通过链接定位的详细内容；
5. 是否包含敏感信息、本地临时状态或无证据结论；
6. 新任务能否通过 `AGENTS.md`、`PROJECT_GUIDE.md` 和 `PROJECT_STATUS.md` 三个入口快速建立可靠上下文。
